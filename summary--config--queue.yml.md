615
Solid Queue. 3 priority lanes: critical, default, low. SOLID_QUEUE_THREADS threads (default 5), JOB_CONCURRENCY processes (default 1). Polling 0.1s. Production inherits default. Test uses queues: "*" with 1 thread.
