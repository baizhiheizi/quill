#!/bin/bash -e

prepare_databases() {
  ./bin/rails db:prepare
}

if [ "${1}" = "./bin/rails" ] && [ "${2}" = "server" ]; then
  prepare_databases
elif [ "${1}" = "bin/jobs" ]; then
  prepare_databases
fi

exec "${@}"
