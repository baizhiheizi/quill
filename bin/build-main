#!/usr/bin/env bash

# throw error when failed
set -e

yarn install
bundle install

# compile assets
yarn build
yarn build:css
bundle exec bin/rails assets:precompile

# clean assets
bundle exec bin/rails assets:clean
