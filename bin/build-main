#!/usr/bin/env bash

# throw error when failed
set -e

npm install
bundle install

# compile assets
npm run build
npm run build:css
bundle exec bin/rails assets:precompile

# clean assets
bundle exec bin/rails assets:clean
