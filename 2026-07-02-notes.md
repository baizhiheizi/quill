# Test Improver Run — 2026-07-02

- Branch: `test-assist/user-authorization-coverage`, commit `6b70069`
- 11 tests / 35 assertions / 0 failures / 0 errors / 0 skips
- `bin/rubocop` clean (478 files), `bin/rails zeitwerk:check` all is good
- `bin/rails test test/models/user_authorization_test.rb` clean (11 / 35)
- `bin/rails test test/models/` clean (397 / 971 / 0F / 0E / 1 pre-existing skip)
- Draft PR via safeoutputs patch mode (bundle at `/tmp/gh-aw/aw-test-assist-user-authorization-coverage.bundle`, 4.0 KB; patch `.patch`, ~10.6 KB)

## Coverage

`UserAuthorization` test file was an empty placeholder. Filled with:

- `store_accessor` for `phone / key / contract / avatar_url / name / biography` — read and write through `raw`.
- `provider` enum stringifies to `mixin / fennec / mvm_eth / twitter`.
- Validations: `provider / raw / uid` required; `uid` uniqueness scoped to `provider` (same uid may exist under a different provider).
- `belongs_to :user, optional: true` — auth rows may exist before the User is provisioned (the `find_or_create_user_by_auth` flow relies on this).
- `refresh!` early-returns for `:twitter` (no API call, no DB write). Otherwise calls `QuillBot.api.user(raw["user_id"])` and merges the returned `data` into `raw`.
- `has_safe?` short-circuits on a truthy `raw["has_safe"]` (no refresh); falls through to `refresh!` when the flag is absent or false.

## Gotchas (new this run)

- **`QuillBot.define_singleton_method(:api) { ... }` block evaluates under `QuillBot`, not the test instance.** Critical for `teardown` restore — `{ @previous_quill_bot_api }` resolves to `QuillBot.@previous_quill_bot_api` (always nil). Capture into a local: `previous_api = @previous_quill_bot_api; QuillBot.define_singleton_method(:api) { previous_api }`. Applies to any module/class `define_singleton_method` teardown restoration.
- **`test/models/transfer_test.rb` lines 220-229 has a class-pollution bug.** Swaps `UserAuthorization#has_safe?` then `remove_method`s the original in `ensure` instead of restoring — leaves the class without `has_safe?` for any later test in the same process. Fix would be to capture `instance_method(:has_safe?)` up front and `define_method(:has_safe?, bound_method)` to restore. Out of scope for this PR; mitigated by `ORIGINAL_HAS_SAFE = UserAuthorization.instance_method(:has_safe?)` at file load and `define_method(:has_safe?, ORIGINAL_HAS_SAFE)` in setup.
- **`Bonus` AASM still blocked** — `Bonus.table_name` returns `"bonus"` (Rails class-name inference; no `self.table_name =` in the model file), but `db/schema.rb` defines `"bonuses"`. PG::UndefinedTable on any real `Bonus.create!`.
- **`belongs_to :user` and `provider` enum are also exercised in `user_test.rb`** — kept assertions here too so `user_authorization_test.rb` stays self-contained as a contract spec.

## Next-run candidates

- `MixinNetworkUser` model — empty test file. Complex (`encrypted_pin`, `before_validation :setup_attributes` calling `QuillBot.api.create_user`). Heavy stubs.
- `Bonus` AASM — still blocked by table-name bug (see above).
- `NftCollection.icon_url` — single-method model. Low value.

## Older completed runs (summary)

- 2026-06-29: Announcement coverage (17, `5ed39f2`)
- 2026-06-26: NotificationSetting coverage (14, `faa4ec8`)
- 2026-06-25: Transfer model coverage (33, `8d069bd`)
- 2026-06-24: SwapOrderFinishedNotifier (12, `f31381f`)
- 2026-06-21: PaymentRefundedNotifier (7, `adac7f8`)
- 2026-06-20: SwapOrderSwappingNotifier (9, `1704bf6`, PR #1707 merged)
- 2026-06-19: CollectionListedNotifier (17, `7e48232`)
- 2026-06-17: CollectionBoughtNotifier (13, `cc8954e`)
- 2026-06-14 r2: SwapOrderFinishedNotifier (9, `af44efd`)
- 2026-06-14 r1: distribute_article_order! memo+wallet (11, `3c2a67c`)
- 2026-06-12: distribute_collection_order! (15, `841f690`)
- 2026-06-11: collect_early_readers (5, `c825d82`)
- 2026-06-09: ArticlePolicy (23) + Articles::Purchasable (5, `5d1560d`)
- 2026-06-08: PreOrder state machine (18, PR #1545 merged)
- 2026-06-07: MarkdownRenderService (21) + RichTextRenderService (17) — PR #1542 closed
- 2026-06-01: PR #1516 (CiterReference) and PR #1519 (Payment memo + policy) merged
