---
description: Project context and high-level guidance for Quill
alwaysApply: true
---

Quill is a Rails 8 Web3 paid-publishing platform with early-reader revenue sharing. Authors publish priced articles; readers pay via crypto (Mixin, MixPay); platform takes ~10%, early readers ~40%, author the remainder.

When making changes, always:
- Keep diffs focused; match existing Rails patterns (frozen string literal, snake_case, concerns)
- Do not commit secrets (`master.key`, `settings.local.yml`, credential values)
- Run `bin/rubocop` for Ruby and `bun run lint-check` for JS when touching those areas
- Prefer extending existing models/jobs/services over parallel implementations
- Read `AGENTS.md` for setup commands, architecture, and domain gotchas (revenue ratios, launch gate, API auth)

Key domains: `Article`, `Order`, `Transfer`, `User`, `Collection`, `PreOrder`, Mixin/MixPay payment flows, Solid Queue workers (`bin/jobs`).
