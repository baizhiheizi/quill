2205
API::ArticlesController. authenticate_user! only create. index: branch on author_id/current_user/only_published; ransack OR title+intro+tags; order=asc|desc or order_by_popularity; limit 20 capped 100; offset integer or ISO8601 cursor. show: find_by!(uuid); Pundit show?. create: authorize, current_user.articles.new(article_params.merge(source: token.value)), CreateTagService, publish! if may_publish?, render_created {uuid:}. article_params: title,intro,content,price,asset_id nested under :article.
