2205
API::ArticlesController. index filters by author_id/current_user/only_published, Ransack OR on title+intro+tags, order=asc|desc or order_by_popularity, limit 20 (cap 100), offset int or ISO8601 cursor. show: find_by!(uuid), Pundit show?. create: authorize, current_user.articles.new(article_params.merge(source:token)). CreateTagService.call, publish! if may_publish?, returns {uuid:} or 422. params.require(:article).permit(:title,:intro,:content,:price,:asset_id).
