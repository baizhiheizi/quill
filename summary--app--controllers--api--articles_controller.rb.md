API::ArticlesController. QUERY_LENGTH_LIMIT=64 (trim+comma-split). index branches author_id/current_user/only_published; ransack OR title+intro+tags; limit 20 capped 100; order=asc|desc or order_by_popularity; offset int or ISO8601 cursor. show: find_by!(uuid); Pundit show?. create: authorize, source=token.value, CreateTagService, publish! if may_publish?, render_created {uuid:}. article_params: title,intro,content,price,asset_id under :article.
