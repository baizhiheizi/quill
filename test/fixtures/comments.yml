# == Schema Information
#
# Table name: comments
#
#  id               :bigint           not null, primary key
#  commentable_type :string
#  comments_count   :integer          default(0)
#  content          :string
#  deleted_at       :datetime
#  downvotes_count  :integer          default(0)
#  upvotes_count    :integer          default(0)
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#  author_id        :bigint
#  commentable_id   :bigint
#  quote_comment_id :bigint
#
# Indexes
#
#  index_comments_on_author_id                            (author_id)
#  index_comments_on_commentable_type_and_commentable_id  (commentable_type,commentable_id)
#  index_comments_on_quote_comment_id                     (quote_comment_id)
#

one:
  author: author
  commentable: published_paid (Article)
  legacy_markdown_content: "This is a test comment"
  created_at: <%= 1.day.ago %>
  updated_at: <%= 1.day.ago %>

two:
  author: reader_two
  commentable: published_paid (Article)
  legacy_markdown_content: "This is another test comment"
  quote_comment: one
  created_at: <%= 1.day.ago %>
  updated_at: <%= 1.day.ago %>
