<%# specs/011-comprehensive-ui-refactor — design-system reference: masthead.
    Renders an isolated masthead via the shared partial inside a frame so it
    doesn't fight with the parent page's chrome. %>
<div class="ds-masthead-example overflow-hidden rounded-[10px] border border-base-300">
  <%= render "shared/masthead" %>
</div>