<%# specs/011-comprehensive-ui-refactor — design-system reference: page shell. %>
<div class="ds-page-shell-example overflow-hidden rounded-[10px] border border-base-300">
  <div class="bg-base-100 px-6 py-12">
    <p class="eyebrow mb-2 font-mono text-[13px] text-base-content/60">SAMPLE PAGE</p>
    <h1 class="font-display text-4xl">Reading measure</h1>
    <p class="mt-3 max-w-[68ch] text-base-content/60">Body copy inherits the sans stack. The article body is capped at a comfortable ~68ch via <code class="font-mono text-xs text-primary">max-w-[68ch]</code>; chrome stays slim.</p>
  </div>
</div>