<!DOCTYPE html>
<% @page_title = @page_title.present? ? format('%<title>s | %<default>s', title: @page_title, default: Settings.page_title) : Settings.page_title %>
<% @page_description ||= Settings.page_description %>
<% @page_keywords ||= Settings.page_keywords %>
<% @page_image ||= asset_url(Settings.logo_file) %>

<html>
  <head>
    <title><%= @page_title.presence || Settings.page_title %></title>
    <meta
      name='viewport'
      content='minimum-scale=1, initial-scale=1, width=device-width, user-scalable=no, shrink-to-fit=no'
    />

    <meta name='title' content="<%= @page_title %>">
    <meta name='keywords' content="<%= @page_keywords %>">
    <meta name='description' content="<%= @page_description %>">

    <meta name="twitter:card" content="summary">
    <meta name="twitter:site" content="@prsdigg" />
    <meta name="twitter:creator" content="@prsdigg" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:title" content="<%= @page_title %>" />
    <meta property="og:description" content="<%= @page_description %>" />
    <meta property="og:image" content="<%= @page_image %>" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <% if Rails.env.production? && Settings.ga_id.present? %>
      <script async src="<%= 'https://www.googletagmanager.com/gtag/js?id=' + Settings.ga_id.to_s %>" ></script>
      <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', '<%= Rails.application.credentials[:ga_id].to_s %>');
      </script>
    <% end %>

    <%= favicon_link_tag "#{Settings.favicon_file || 'favicon.png'}" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="min-h-screen flex flex-col dark:text-zinc-50">
    <%= render "shared/nav" %>
    <main class="bg-zinc-100 dark:bg-zinc-800 relative mt-12 sm:mt-16 flex-1">
      <div class="max-w-3xl sm:pt-4 pb-8 mx-auto sm:px-6 lg:max-w-6xl lg:px-8">
        <% flash.each do |type, message| %>
          <%= render partial: "shared/flash", locals: { type: type, message: message } %>
        <% end %>
        <div class="px-4 pt-4 sm:pl-12 sm:p-0 text-lg sm:text-xl font-bold mb-4">
          <%= t('dashboard') %>
        </div>
        <div class="lg:grid lg:grid-cols-5 lg:gap-x-3">
          <div class="lg:col-span-1">
            <%= render "dashboard/menu" %>
          </div>
          <div class="bg-white dark:bg-zinc-900 dark:border-zinc-800 sm:rounded-lg w-full grid-span-1 lg:col-span-4">
            <%= yield %>
          </div>
        </div>
      </div>
      <div style="height:env(safe-area-inset-bottom)" />
    </main>
    <div class="absolute bottom-0" id="modal-slot"></div>
    <div class="fixed top-8 h-0 w-full px-4" id="notifications"></div>
    <div class="absolute bottom-0" id="toast-slot">
      <div id="loading-toast" class="hidden fixed inset-0 z-50 flex items-center justify-center w-full bg-black opacity-30">
        <%= render "shared/loading" %>
      </div>
    </div>
    <%= turbo_stream_from "user_#{current_user.mixin_uuid}" if current_user.present? %>
  </body>
</html>
