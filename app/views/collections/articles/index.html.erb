<%= turbo_frame_tag "articles" do %>
  <div data-controller="infinite-scroll">
    <% if @articles.blank? %>
      <%= render_empty text: t('no_record') %>
    <% else %>
      <div id="<%= dom_id @collection %>_articles_list">
        <%= render partial: "articles/card", collection: @articles, as: :article %>
      </div>
      <div data-infinite-scroll-target='scrollArea'></div>

      <%= render_pagination @pagy, id: "#{dom_id @collection}_articles_pagination" %>
    <% end %>
  </div>
<% end %>
