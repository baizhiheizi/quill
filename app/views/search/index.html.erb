<%# /search is the live-search fragment endpoint (see index.turbo_stream.erb);
    this page covers direct visits so a plain HTML GET renders the same search
    box instead of raising UnknownFormat (406). %>
<div class="mx-auto w-full max-w-xl px-4 py-8">
  <%= render "search/form" %>
</div>
