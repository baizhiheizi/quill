<nav class="hidden bg-white sm:block border-b fixed top-0 w-full z-50">
  <div class="px-4 px-8 mx-auto max-w-7xl">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: 'flex items-center space-x-2 flex-shrink-0' do %>
          <img class="w-8 h-8" src=<%= asset_path('logo.png') %> alt="Logo">
        <% end %>
        <div class="flex items-baseline ml-10 space-x-4">
          <%= link_to t("read"), articles_path, class: "px-3 py-2 text-base font-medium hover:bg-blue-50 hover:text-blue-500 rounded-md" %>
        </div>
        <div class="flex items-baseline ml-10 space-x-4">
          <% if current_user.present? %>
            <%= link_to t("write"), new_article_path, class: "px-3 py-2 text-base font-medium hover:bg-blue-50 hover:text-blue-500 rounded-md" %>
          <% else %>
            <%= link_to t("write"), view_modals_path(type: :login), data: { turbo_method: :post }, class: "px-3 py-2 text-base font-medium hover:bg-blue-50 hover:text-blue-500 rounded-md" %>
          <% end %>
        </div>
        <div class="flex items-baseline ml-10 space-x-4">
          <%= link_to t("rules"), page_path('rules'), class: "px-3 py-2 text-base font-medium hover:bg-blue-50 hover:text-blue-500 rounded-md" %>
        </div>
      </div>
      <div class="flex items-center ml-4 md:ml-6">
        <% if current_user.present? %>
          <%= link_to notifications_path, class: "p-1 relative rounded-full hover:text-blue-500 hover:bg-blue-50" do %>
            <span class="sr-only">View notifications</span>
            <!-- Heroicon name: outline/bell -->
            <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
            </svg>
            <% if current_user.has_unread_notification? %>
              <span class="absolute top-0 right-0 rounded-full bg-red-500 w-2 h-2"></span>
            <% end %>
          <% end %>
        <% end %>

        <!-- Profile dropdown -->
        <% if current_user.present? %>
          <div data-controller="dropdown" class="relative ml-3">
            <div>
              <button data-action="dropdown#toggle click@window->dropdown#hide" 
                type="button" 
                class="flex items-center max-w-xs text-sm rounded-full focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-blue-500 focus:ring-white" 
                id="user-menu-button" 
                aria-expanded="false" 
                aria-haspopup="true">
                <span class="sr-only">Open user menu</span>
                <img class="w-8 h-8 rounded-full" src=<%= current_user.avatar %> alt="">
              </button>
            </div>
            <div data-dropdown-target="menu" 
                data-transition-enter-active="transition ease-out duration-100"
                data-transition-enter-from="transform opacity-0 scale-95"
                data-transition-enter-to="transform opacity-100 scale-100"
                data-transition-leave-active="transition ease-in duration-75"
                data-transition-leave-from="transform opacity-100 scale-100"
                data-transition-leave-to="transform opacity-0 scale-95"
                class="absolute right-0 hidden w-48 py-1 mt-2 bg-white shadow-lg origin-top-right rounded-md ring-1 ring-black ring-opacity-5 focus:outline-none" 
                role="menu" 
                aria-orientation="vertical" 
                aria-labelledby="user-menu-button" 
                tabindex="-1">
                <%= link_to t("dashboard"), user_path(current_user), class: "block px-4 py-2 text-sm text-gray-700 active:bg-gray-100", role: "menuitem", tabindex: "-1" %>
                <%= link_to t("logout"), logout_path, class: "block px-4 py-2 text-sm text-gray-700 active:bg-gray-100", role: "menuitem", tabindex: "-1" %>
              </div>
            </div>
          <% else %>
            <%= link_to 'Connect Wallet', view_modals_path(type: :login), data: { turbo_method: :post }, class: "hover:bg-blue-600 bg-blue-500 text-white px-3 py-2 rounded" %>
          <% end %>
        </div>
    </div>
  </div>
</nav>

<nav class="bg-white sm:hidden border-b w-full fixed top-0 z-50" data-controller="dropdown">
  <div class="px-4 px-6 mx-auto">
    <div class="relative flex items-center justify-center h-12">
      <div class="absolute left-0">
        <button data-action="dropdown#toggle click@window->dropdown#hide" type="button" class="inline-flex items-center justify-center p-2 rounded-md hover:text-blue-500 hover:bg-blue-50 focus:bg-blue-50 focus:text-blue-500" aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Open main menu</span>
          <svg class="block w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
      <%= link_to root_path, class: "flex items-center flex-shrink-0 space-x-2" do %>
        <img class="w-8 h-8" src=<%= asset_path('logo.png') %> alt="Logo">
        <span class='text-xl font-medium'><%= t("app_name") %></span>
      <% end %>
    </div>
    <!-- Mobile menu, show/hide based on menu state. -->
    <div data-dropdown-target="menu">
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
        <%= link_to t("read"), articles_path, class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
      </div>
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
        <% if current_user.present? %>
          <%= link_to t("write"), new_article_path, class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
        <% else %>
          <%= link_to t("write"), view_modals_path(type: :login), data: { turbo_method: :post }, class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
        <% end %>
      </div>
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
        <%= link_to t("rules"), page_path('rules'), class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
      </div>
      <div class="pt-4 pb-3">
        <% if current_user.present? %>
          <div class="flex items-center px-5">
            <div class="flex-shrink-0">
              <img class="w-10 h-10 rounded-full" src=<%= current_user.avatar %> alt="user avatar">
            </div>
            <div class="ml-3">
              <div class="text-base font-medium leading-none text-white"><%= current_user.name %></div>
            </div>
            <%= link_to notifications_path, class: "relative flex-shrink-0 p-1 ml-auto rounded-full focus:bg-blue-50 focus:text-blue-500" do %>
              <span class="sr-only">View notifications</span>
              <!-- Heroicon name: outline/bell -->
              <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
              <% if current_user.has_unread_notification? %>
                <span class="absolute top-0 right-0 rounded-full bg-red-500 w-2 h-2"></span>
              <% end %>
            <% end %>
          </div>
          <div class="px-2 mt-3 space-y-1">
            <%= link_to t("dashboard"), user_path(current_user), class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
          </div>
          <div class="px-2 mt-3 space-y-1">
            <%= link_to t("logout"), logout_path, class: "block px-3 py-2 text-base font-medium focus:bg-blue-50 focus:text-blue-500 rounded-md" %>
          </div>
        <% else %>
          <div class="px-2 sm:px-3 flex">
            <%= link_to 'Connect Wallet', view_modals_path(type: :login), data: { turbo_method: :post }, class: "block px-3 py-2 text-base font-medium text-white bg-blue-500 focus:bg-blue-600 rounded-md" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
