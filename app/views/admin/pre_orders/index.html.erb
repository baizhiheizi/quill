<div class="flex items-center space-x-4 mb-4">
  <%= link_to 'Home', admin_root_path %>
  <span>/</span>
  <span>PreOrders</span>
</div>

<div class="p-4 bg-white dark:bg-zinc-900 rounded-lg shadow">
  <%= turbo_frame_tag 'admin_pre_orders' do %>
    <div class="mb-4">
      <%= render 'admin/pre_orders/query' %>
    </div>
    <div data-controller="infinite-scroll" class="w-full overflow-x-scroll scrollbar-hide">
      <table class='w-full table-auto min-w-max '>
        <thead>
          <tr class="bg-zinc-100 dark:bg-zinc-800">
            <th class="p-4">ID</th>
            <th class="p-4">TraceID</th>
            <th class="p-4">FollowID</th>
            <th class="p-4">Item</th>
            <th class="p-4">Payer</th>
            <th class="p-4">Total</th>
            <th class="p-4">State</th>
            <th class="p-4">OrderType</th>
            <th class="p-4">Created</th>
            <th class="p-4"></th>
          </tr>
        </thead>
        <tbody id="admin_pre_orders_list">
          <%= render partial: 'admin/pre_orders/pre_order', collection: @pre_orders, as: :pre_order %>
        </tbody>
      </table>
      <div data-infinite-scroll-target='scrollArea'></div>
      <%= render partial: "shared/pagination", locals: { pagy: @pagy } %>
    </div>
  <% end %>
</div>
