<%= turbo_stream.update "notifications" do %>
  <% if @bonus.errors.present? %>
    <%= render partial: "notifications/notification", locals: { type: :danger, message: @bonus.errors.full_messages.join(';') } %>
  <% else %>
    <%= render partial: "notifications/notification", locals: { type: :success, message: 'Created' } %>
  <% end %>
<% end %>

<% if @bonus.errors.blank? %>
  <%= turbo_stream.update "modal-slot" do %>
  <% end %>

  <%= turbo_stream.prepend "admin_bonuses_list" do %>
    <%= render 'admin/bonuses/bonus', bonus: @bonus %>
  <% end %>
<% end %>
