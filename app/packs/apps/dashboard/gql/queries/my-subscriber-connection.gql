query MySubscriberConnection($after: String) {
  mySubscriberConnection(after: $after) {
    nodes {
      id
      mixinId
      name
      avatar
      bio
      subscribed
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
