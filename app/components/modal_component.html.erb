<div 
  class="modal"
  data-controller="modal-component" 
  data-action="turbo:submit-end->modal-component#submitEnd"
  data-modal-component-backdrop-value="<%= @backdrop %>"
  data-modal-component-initial-state-value="show">
  <div data-modal-component-target="wrapper" class="fixed inset-0 z-50 flex items-start justify-center w-full">
    <button data-modal-component-target="backdrop" data-action="modal-component#backdropClicked" class="fixed top-0 left-0 w-screen h-full bg-black dark:bg-white opacity-40"></button>
    <div data-modal-component-target="modal" 
      data-transition-enter-from="opacity-0 transition-y-0"
      data-transition-enter-to="opacity-100 transition-y-6"
      data-transition-leave-from="opacity-100"
      data-transition-leave-to="opacity-0 transition-y-0"
      class="<%= @classes %> relative w-full sm:w-[500px] max-w-2xl min-h-screen-1/3 sm:min-h-4 sm:h-auto max-h-screen-3/4 mx-auto mt-auto sm:my-auto bg-white dark:bg-zinc-900 rounded-t-xl sm:rounded-xl shadow overflow-y-auto sm:scrollbar-hide">
      <div data-modal-component-target="title" 
        class="sticky w-full top-0 flex items-center justify-between px-8 py-7 text-base font-bold space-x-4 bg-white dark:bg-zinc-900 dark:text-zinc-50 z-10 rounded-t-xl">
        <span><%= @title %></span>
        <button data-action="modal-component#hide">
          <%= inline_svg_tag "icons/close-solid.svg", class: "w-6 h-6 opacity-30" %>
        </button>
      </div>

      <div data-modal-component-target="content" class="px-8 pt-4 pb-8">
        <%= content %>
        <div style="height:env(safe-area-inset-bottom)"></div>
      </div>
    </div>
  </div>
</div>
