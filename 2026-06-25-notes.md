# Test Improver Run — 2026-06-25

- Branch: `test-assist/transfer-model-coverage`, commit `8d069bd`
- 35 tests / 83 assertions / 0 failures / 0 errors / 1 documented skip
- `bin/rubocop` clean, `bin/rails zeitwerk:check` all is good
- `bin/rails test test/models/` clean (205 runs / 421 assertions / 0 failures / 1 skip)
- PR via safeoutputs patch mode

## Gotchas

- **`Transfer#process_safe_transfer!` calls `check!` first**, sets `processed_at` on success. `create_safe_transfer` only runs on `MixinBot::NotFoundError`.
- **`Bonus` table_name mismatch**: `Bonus.table_name` returns `"bonus"` but migration creates `"bonuses"`. Existing `bonus_test.rb` is empty.
- **`belongs_to` with custom keys** loads a fresh instance — `define_singleton_method` on a fixture instance does NOT propagate. Use `UserAuthorization.define_method(...)` (class-level) instead, restore in `ensure`.
- **`UserAuthorization#has_safe?`**: `raw["has_safe"].present?` is false for both `nil` AND `false`, falls through to `refresh!` (real API).
- **`process!` source dispatch**: Payment → `payment.refund! if payment.may_refund?` (need `:paid`); Bonus → `bonus.complete! if bonus.may_complete?`.

## Next-run candidates

- **`Bonus` AASM** (blocked by table-name bug), `NftCollection.icon_url`, `MixinNetworkUser` (heavy stubs)
- **`ArticleSearchService`** — 50+ lines of complex query logic, deeper edge-case testing possible.

## Older runs (summary)

- 2026-06-24: SwapOrderFinishedNotifier (12, `f31381f`)
- 2026-06-21: PaymentRefundedNotifier (7, `adac7f8`)
- 2026-06-20: SwapOrderSwappingNotifier (9, `1704bf6`, PR #1707 merged)
- 2026-06-19: CollectionListedNotifier (17, `7e48232`)
- 2026-06-17: CollectionBoughtNotifier (13, `cc8954e`)
- 2026-06-14 r2: SwapOrderFinishedNotifier (9, `af44efd`)
- 2026-06-14 r1: distribute_article_order! memo+wallet (11, `3c2a67c`)
- 2026-06-12: distribute_collection_order! (15, `841f690`)
- 2026-06-11: collect_early_readers (5, `c825d82`)
- 2026-06-09: ArticlePolicy edge cases (23) + Articles::Purchasable (5, `5d1560d`)
- 2026-06-08: PreOrder state machine (18, PR #1545 merged)
- 2026-06-07: MarkdownRenderService (21) + RichTextRenderService (17) — PR #1542 closed
- 2026-06-01: PR #1516 (CiterReference) and PR #1519 (Payment memo + policy) merged