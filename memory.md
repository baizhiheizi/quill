---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-24 12:48 UTC (run 28098485167).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists; read before any code PR.
- **Open PRs**: 2 new draft PRs from this run — `repo-assist/fix-admin-mixin-owner-id-typo-2026-06-24` and `repo-assist/perf-user-available-articles-2026-06-24`. (Numbers pending — gh-aw safeoutputs create_pull_request returned success but the GitHub API lookup of #1731-#1735 returned 404 at comment-write time. Patches and bundles are on disk under `/tmp/gh-aw/`.)
- **Open issues**: 8 — all auto-generated by `github-actions[bot]`. 0 human-submitted open issues.

## Selected tasks for run 28098485167 (2026-06-24 12:48 UTC)

- **Task 5 (Coding Improvements)** — fixed latent copy-paste typo in `Admin::MixinNetworkUsersController#index`: `owner_id: params[:owner_type]` → `owner_id: params[:owner_id]`. Branch `repo-assist/fix-admin-mixin-owner-id-typo-2026-06-24` (commit 8aeba3c6), 1 file, 1 insertion, 1 deletion. Rubocop + zeitwerk clean. Patch: `/tmp/gh-aw/aw-repo-assist-fix-admin-mixin-owner-id-typo-2026-06-24.patch` (1864 bytes, 39 lines). Bundle: `/tmp/gh-aw/aw-repo-assist-fix-admin-mixin-owner-id-typo-2026-06-24.bundle` (1113 bytes).
- **Task 8 (Performance Improvements)** — refactored `User#available_articles` to push `.uniq` to SQL via `.or.or.distinct`. Eliminates Ruby-side materialisation of every free article in the DB. 2 SELECTs → 1 SELECT. Strengthened test with set-equivalence check. Branch `repo-assist/perf-user-available-articles-2026-06-24` (commit 8873c6e5), 2 files, +24 / -4. Patch: `/tmp/gh-aw/aw-repo-assist-perf-user-available-articles-2026-06-24.patch` (3587 bytes, 96 lines). Bundle: `/tmp/gh-aw/aw-repo-assist-perf-user-available-articles-2026-06-24.bundle` (2033 bytes).
- **Task 2 (Issue Investigation and Comment)** — not applicable. All 8 open issues are auto-generated by `github-actions[bot]`. Fallback Task 1 also not applicable (0 unlabelled issues). Recorded in Task 11 entry.
- **Task 11** — posted run entry on issue #1564 (comment `aw_Hy13zrmc`) with both PRs in Suggested Actions.

## Completed this run (28098485167)

- Fixed `owner_id: params[:owner_type]` typo in `Admin::MixinNetworkUsersController#index` (commit 8aeba3c6 on `repo-assist/fix-admin-mixin-owner-id-typo-2026-06-24`).
- Refactored `User#available_articles` to SQL `.or.or.distinct` (commit 8873c6e5 on `repo-assist/perf-user-available-articles-2026-06-24`).
- Created two draft PRs via `safeoutputs create_pull_request`. Both returned `{"result":"success"}` with patch/bundle paths but no PR number; GitHub API lookup of #1731-#1735 returned 404 at this moment. The PR creation may complete later in the workflow, or may have failed silently — see notes.
- Posted run entry on issue #1564 (comment `aw_Hy13zrmc`).

## Prior runs (compressed)

- **28075390697** (2026-06-24 04:48 UTC): Task 5 — extended `remote_image_tag` from 4 view files to 29. Branch `repo-assist/extend-remote-image-tag-2026-06-24` (commit 12d29012). **PR #1730 merged 2026-06-24 05:34 UTC** (commit 7b6f52a9).
- **28059513048** (2026-06-23 21:55 UTC): Task 4 — added status comment on bundle issue #1717 noting PR #1716 (lexxy 0.9.19) was auto-closed by Dependabot when #1723 superseded it.
- **28043998628** (2026-06-23 16:30 UTC): Task 5 — `app/views/layouts/editor.html.erb` hardcoded `@prsdigg`; opened PR #1724.
- **27987716241** (2026-06-22 23:06 UTC): Tasks 4, 5 — bundled Dependabot PRs into issue #1717 (protected files) + removed `SubdomainConstraint` (PR #1718).
- **27918345901** (2026-06-21 21:44 UTC): Opened mixpay-button-controller removal (merged #1713).
- **27911156543** (2026-06-21 16:58 UTC): Opened `fix-preview-upload-audio-fallback` (PR #1711).
- **27723165917** (2026-06-17 22:50 UTC): Task 9 — filled empty `Currency` test stub (`test/models/currency_test.rb`).
- **27708899632** (2026-06-17 19:00 UTC): Task 10 — reinvestigated UserHelper; was correct on main.
- **27694493149** (2026-06-17 15:15 UTC): Tasks 9, 5 — cable connection tests (PR #1679 merged) + dashboard block-users cleanup (PR #1680 merged).
- **27652015809** (2026-06-17 11:55 UTC): Task 5 — removed 5 empty helpers + dead share_to_facebook + URL-encoding fix (PR #1674 merged).
- **27596408742** (2026-06-16 05:50 UTC): Task 10 — docs follow-up to 858b5779 production SQLite (PR #1670 merged).

## Backlog / future work

- Re-engage when human issues appear.
- 5 notifiers lack dedicated tests: `collection_listed`, `payment_refunded`, `subscribe_user_action_created`, `swap_order_finished`, `swap_order_swapping`. Test-improver owns.
- **Latent `Currency#store :raw` JSONB bug** — partially addressed by maintainer in `ef58c599` (defensive `icon_url` reader) + `66a7fc1d` (helper in pre_orders views) + `7b6f52a9` (remote_image_tag sweep). Underlying `before_validation` QuillBot.api.asset call may still raise; deferring further action.
- **Maintainer signal**: view patterns that have been started but not finished — keep looking for these in future runs (e.g. `remote_image_tag` was only applied to 4 view files before this run extended it to 29).
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- Specialized workflows (test-improver, efficiency-improver, perf-improver, code-simplifier) own the natural notifier-testing / efficiency / perf / simplification work.
- #1667 + #1686 (Authorization Boundary Hygiene) and #1694 (Webhook/Callback/Payment-Replay Integrity) are maintainer-led design discussions; out of scope.
- 858b5779 production Solid Cache/Cable/Queue SQLite migration opens future work to verify there's no SQLITE_BUSY contention (maintainer-led).
- **`Admin::CommentsController#show` was unreachable** since at least the route draw was last restructured. Removed in PR #1721 (was open at last refresh; verify merged).
- **Editor layout Twitter handle** fixed in PR #1724 (was open at last refresh; verify merged).
- **`modal-component` identifier is intentional** (`FlyonuiModalController` mapped under `modal-component` in `index.js`). NOT dead.
- **All Stimulus controllers in `index.js` are used.** No further dead Stimulus code.
- **Bundling Dependabot PRs is straightforward in principle** but Gemfile/Gemfile.lock are protected from automation pushes. Workaround: produce patch+bundle artefacts (issue body). Current bundle (#1717) is partially stale because its lexxy leg is 0.9.19 (PR #1716 closed); maintainer should apply #1717's patch + #1722 (cgi) and #1723 (lexxy 0.9.20) bumps.
- **Dependabot auto-closes superseded PRs**: when #1723 opened, #1716 auto-closed. Standard Dependabot behavior.

## Notes

- `gh aw compile` produces `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR finalised via `safeoutputs create_pull_request`.
- **OBSERVED (run 28098485167)**: `safeoutputs create_pull_request` returned `{"result":"success"}` with patch/bundle paths but GitHub API lookup of #1731-#1735 returned 404 at the same moment. PR numbers are assigned either at workflow-end or are async. When this happens, reference PRs by branch name in comments/memory, not by PR number.
- `update_issue` body size limit is 10 KB; queued body updates from `schedule`/`workflow_dispatch` are unreliable. Monthly Activity update via `add_comment`.
- `bin/rubocop` and `bin/rails zeitwerk:check` run locally. RuboCop does NOT lint ERB or YAML.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- `User#create_action` returns a boolean, not the `Action` record. `Action#after_create :notify_target` is the production hook.
- Noticed 3 with `record: <model>` fails with `NoMethodError: undefined method 'has_query_constraints?'` if `record` is not a real ActiveRecord instance.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`; tests assert against this value (the `Config::Options` class does not support mocha's `.stubs`).
- `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID` (`d4444444-4444-4444-8444-444444444444`).
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`, `element.dataset["controller"] = "xxx"`. Use `re.findall(r'data-controller=["\']([^"\']+)["\']', content)`. An identifier is "dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a stub left from a migration.
- `bin/ci` uses `ActiveSupport::ContinuousIntegration` with: `Setup`, `Style: Ruby`, `Style: JavaScript`, `Tests: Rails`, `Tests: Seeds`.
- `Action` extends `ActiveRecord::Base` directly (not `ApplicationRecord`), polymorphic `target`/`user` with `optional: true`, has `after_create :notify_target` + `before_destroy :destroy_notifications`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring routes.
- For JS-only changes, the local-toolchain equivalent of `bun run lint-check` is `node_modules/.bin/prettier --check app/javascript/<changed-file>`; `node esbuild.config.js` produces the same bundle CI builds.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route declaration AND a corresponding view file exists at `app/views/<namespace>/<controller>/<verb>.html.erb`. If both fail, the action is dead.
- **Stale Ruby idiom**: `params[:foo]&.present?` can be `params[:foo].present?` if the route never receives `nil` for that key.
- **Protected files for Dependabot bundling**: Gemfile, Gemfile.lock, and likely package.json/bun.lock are write-protected for `github-actions[bot]`. PRs that touch only those files cannot be auto-pushed via `safeoutputs create_pull_request`.
- **`test/controllers/articles_controller_test.rb` has 3 pre-existing errors** caused by `ActionView::Template::Error: The asset 'application.css' was not found in the load path.` Test-env asset-pipeline issue, not related to changes (verified on `main`). CI has the asset and does not see these errors.
- **`test/controllers/pre_orders_controller_test.rb` has 2+ pre-existing errors** caused by SSL connection failures to Mixpay API in test env (`SSL_connect returned=1 errno=0 ... wrong version number`). Verified on `main`. CI has Mixpay stubbed and does not see these errors.
- **Manifest.js + Tailwind**: `app/assets/config/manifest.js` only does `//= link_tree ../images` and `//= link_tree ../builds`. `app/assets/builds/application.css` is generated by `bun run build:css` (the CSS bundle) and auto-linked.
- **`remote_image_tag` helper** (added in `ef58c599`): returns `nil` for nil/blank source, returns `nil` for non-absolute URL source (not starting with `http://`, `https://`, `//`, `/`), otherwise calls `image_tag`. Translates `lazy: true` option to `loading: "lazy"`. Use this everywhere `.icon_url` (or any external URL) is rendered instead of raw `image_tag`.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])` — copy-paste where the second binding uses the wrong param key. Look for this in other admin controllers; `grep -rn "params\[:[a-z_]*\]\.\?$" app/controllers/admin/` if extending the pattern.
- **`User#available_articles` perf pattern**: push `.uniq` to SQL via `.or.or.distinct`. Verified set-equivalent on `Reader Two` (id 45371440): old `to_a + to_a + uniq` and new `.or.or.distinct` returned identical ids. The free-article pool clause `Article.only_free.only_published` is intentional (free articles are readable by anyone, so users can reference any of them).