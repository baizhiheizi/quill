---
name: repo-assist-memory
description: Repo Assist run state, completed work, open backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28660097062 on 2026-07-03 12:24 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs**: NONE.
- **Open issues**: 13 (all auto-generated by other AI agents or my own monthly activity issue). New since last run: #1821 (Repository Quality Improver BigDecimal/DistributeService audit), #1823 (perf-improver transfers eager-load proposal, push-blocked, branch `perf-assist/dashboard-transfers-eager-load-20260703` commit `26882f2`), #1824/#1825 (perf-improver monthly), #1816 (efficiency-improver — articles eager-load re-filed after #1815 merged), #1817 (efficiency-improver monthly), #1818 (aw No-Op Runs). No human-submitted issues.
- **Open PRs**: 1 non-Repo-Assist — PR #1822 `Editorial Web3 UI redesign — public pages` (maintainer an-lee, draft, opened 2026-07-03 10:50). Not stale.
- **Recent merges** (this run window):
  - PR #1815 (efficiency-improver articles eager-load) MERGED 2026-07-03 02:32:20 UTC by an-lee.
  - PR #1814 (Documentation Unbloat, docs/local-development.md tightening) MERGED 2026-07-03 02:31:48 UTC.
  - PR #1813 (dependabot lexxy 0.9.21→0.9.22) MERGED 2026-07-02 22:32:22 UTC.

## This run (28660097062)

- **Selected tasks**: Task 2, Task 3, Task 1, plus Task 11.
- **Task 1 (Issue Labelling)**: No-op — all 13 open issues already carry appropriate labels (`automation` / `repo-assist` / `efficiency` / `performance` / `quality` / `testing` / `agentic-workflows` / `dependencies` / `refactor` / `financial-integrity` / `bigdecimal`). No relabelling needed.
- **Task 2**: No-op — all 13 open issues auto-generated. No human-submitted issues need engagement.
- **Task 3**: No-op — `bug` / `help wanted` / `good first issue` search returned 0 open issues. Substituted to Task 2 per fallback rules.
- **Task 11**: Updated #1789. Body at 7670 bytes (under 10 KB cap with margin). Replaced whole body (previous run had duplicated body content from a prior update glitch). Added new run entry at top of Run History with the 12:24 UTC timestamp and the new issues opened by other agents.

## Previous run (28637693543) — see #1789 Run History

- Task 2 (no-op), Task 3 (no-op), Task 5 (deferred — both obvious next-step improvements already exist as local branches). All three merged PRs (#1813, #1814, #1815) confirmed in current state.

## Earlier run (28622852314) — see #1789 Run History

- Task 3 (no-op — auth-name fix already merged as PR #1811), Task 2 (no-op — auto-gen only), Task 5 (deferred articles perf), Task 11.

## Earlier run (28607591885) — see #1789 Run History

- Task 3 (no-op — auth-name fix already merged as PR #1811), Task 2 (no-op — auto-gen only), Task 5 (deferred articles perf), Task 11.

## Earlier run (28589515383) — see #1789 Run History

- Task 3 (auth-name fix, branch `repo-assist/fix-auth-name-existing-user-2026-07-02` commit `4c6f2493`), Task 2 (no-op), Task 10 (articles perf deferred), Task 11. The auth-name fix landed as merged PR #1811 later that day (2026-07-02 13:18:10).

## Backlog

- **Concern testing (1 remaining)**: `rich_text_content` — blocked on whether maintainer wants `RichTextContent` concern split from inline `before_save :track_content_change` callback.
- **Issue #1778** (AGENTS.md codify concern-test convention): still OPEN. PR #1787 push rejected 2026-06-30. Convention enforced in PR review memory only.
- **Open perf** (revival candidates): `Dashboard::PaymentsController#index` (branch `b31a4b47`, efficiency-improver agent) and `Dashboard::TransfersController#index` (branch `f6ea29cf`, repo-assist + branch `perf-assist/dashboard-transfers-eager-load-20260703` commit `26882f2` from issue #1823). Same pattern as #1815 revival succeeded 2026-07-03.
- **Open perf (RESOLVED)**: `Dashboard::ArticlesController#index` — landed as merged PR #1815 on 2026-07-03.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience.
- #1667 + #1686 + #1694 + #1771 + #1790 + #1794-#1798: maintainer-led design discussions, out of scope.
- **Duplicated perf-improver PRs** (#1783 + #1784): both merged; workflow-determinism issue deferred.
- **Issue #1821 (Repository Quality Improver)**: BigDecimal / DistributeService audit with F1-F12 findings. F1/F2/F3 are HIGH priority (BigDecimal precision, duplicate MINIMUM_AMOUNT, multi-wallet Order#all_transfers_generated? guard). Worth a follow-up Task 3 attempt IF maintainer signals interest via the issue or a label. The 5-cycle cooldown on payment/Web3 resilience from #1571 may apply.

## Notes

- `bin/rubocop` does NOT lint ERB/YAML. `bin/rails zeitwerk:check` is the local CI load-path check.
- Minitest 6.0.6: `Object#stub` removed — use `define_singleton_method` + `ensure`.
- `Rails.application.credentials` stub: tiny Struct whose `#dig` returns the configured value, restore in `ensure`.
- `safeoutputs update_issue` body limit: 10 KB hard cap. This run's body was 7670 bytes — within budget. Previous run hit 9588 bytes (also under cap). First attempt ever on #1789 hit 10406 bytes (rejected 2026-07-02 run); workaround was trimming oldest Run History entries.
- `PandoBot::Lake::PairRoutes`: `route_id` MUST be Integer. Use `route_id: 1` in stubs.
- **Concern testing pattern**: one canonical instance per concern (Comment for SoftDeletable, Article for Articles::*, MixinPreOrder for PreOrders::*, Order for Orders::*). Pin decision table for predicates; avoid mocking the callback chain.
- **Test env cache**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)`, `update_columns(association_id: ...)`, even `article.association` re-queries DB and replaces in-memory object. Singleton-method stubs lost.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (SSL to Mixpay). CI stubs them.
- `User#available_articles` perf: push `.uniq` to SQL via `.or.or.distinct` (PR #1735).
- `includes(:currency).sum(...)` antipattern: use `joins(:currency)`. Closed in PR #1737.
- `Currency#save` raises in test env (`QuillBot.api.asset(asset_id)` in `before_validation`). Use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- GitHub MCP `list_pull_requests` defaults to ascending — pass `direction: "desc"`.
- MCP `dependabot alerts`: returns 400 "Pagination using the `page` parameter is not supported". Unreliable.
- **`UserAuthorization` store_accessor quirk (RESOLVED via PR #1811)**: `auth.name` reads `raw["name"]` — but Mixin API populates `full_name`, not `name`. Always use `auth.raw["full_name"]`. Fix landed in PR #1811.
- **ActiveStorage eager-load shape**: `Model.includes(:attached_association, attached_association: :blob)` for `has_one_attached`. Confirmed via `Collection#cover` (PR #1802) and `Article#cover` (PR #1815).
- **Polymorphic nested preload shape**: `.includes(:currency, source: { item: :author })` for `Transfer → source (Order/Payment) → item (Article/Collection) → author`. Confirmed via PR #1802, PR #1815, and the proposed #1823 (push-blocked).
- **No Postgres in this runner**. `bin/rails test` unreproducible locally for DB-touching specs; rely on `bin/rails zeitwerk:check` + `bin/rubocop`, defer to CI.
- **Maintainer-revival pattern (CONFIRMED via PR #1815, 2026-07-03)**: when `safeoutputs create_pull_request` returns success but no PR opens, the maintainer can fetch the local bundle and push it manually. Pattern succeeded twice now (#1811 auth-name, #1815 articles eager-load). Orphan branches with local commits + bundles are valuable when the change is small, low-risk, and aligned with maintainer's recent work. Don't create new branches expecting immediate push, but do create them if they fill a clear gap.
- **`mcp__github__list_issues` output too large**: 95 KB+ for 13 issues. Use the saved file path in the tool result and slice with python.
- **`MCP list_issues` defaults to ALL states**: pass `state: "OPEN"` to filter.
- **body update on monthly issue**: the previous run accidentally left duplicated body content. This run did a full `replace` to clean it up.
