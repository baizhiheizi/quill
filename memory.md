---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-27 16:33 UTC (run 28294779111).**
- Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists; read before any code PR.
- **Open PRs from Repo Assist**: 1 (draft). Branch `repo-assist/test-mixpay-pre-order-model-2026-06-27` (commit `0b1ab138`) — `safeoutputs create_pull_request` returned success but no PR on GitHub yet. Patch + bundle saved to `/tmp/gh-aw/aw-repo-assist-test-mixpay-pre-order-model-2026-06-27.{patch,bundle}` (8671 / 2901 bytes). Same `safeoutputs` no-op pattern as prior action-model branch, which DID land on `main` later as commits `096e76a0` + `546ce939` (PR #1754 / #1753) — out-of-band push appears to happen later.
- **Open issues**: 9 — all auto-generated by `github-actions[bot]`. 0 human-submitted open issues.
- **Dependabot queue**: clear. PR #1736 (annotaterb 4.22.0→4.23.0) merged 2026-06-26. All bundle PRs (#1722 cgi, #1723 lexxy, #1730 mini_racer, #1736 annotaterb) merged individually by `an-lee`. Bundle issue #1717 is now redundant — recommended closure in run 28188169668 and again in this run.

## Selected tasks for run 28294779111 (2026-06-27 16:33 UTC)

- **Task 1 (Issue Labelling)** — DONE (no-op). All 9 open issues auto-generated and already labelled.
- **Task 9 (Testing Improvements)** + **Task 10 (Take Repo Forward)** — DONE together. Added `test/models/mixpay_pre_order_test.rb` (7 tests / 24 assertions). `MixpayPreOrder` is STI subclass of `PreOrder`. Coverage: STI (descends from PreOrder, sets type), `:ensure_mixpay_supported` validation (Article + Collection paths, rejects when asset_id absent from settlement_asset_ids, accepts when present), `pay_url` (HTTPS + host + path), 7 query params (payeeId, settlementAssetId, quoteAssetId, quoteAmount, traceId, settlementMemo, returnTo), returnTo resolves to `pre_order_url(follow_id)`. Branch `repo-assist/test-mixpay-pre-order-model-2026-06-27`, commit `0b1ab138`, 1 file, +204/-0. `bundle exec rails test test/models/mixpay_pre_order_test.rb` → 7/24/0/0/0. Full `test/models/` → 254/586/0/0/1 (pre-existing skip). `bundle exec rubocop` → no offenses. `bundle exec rails zeitwerk:check` → all is good.
- **Task 11** — posted run entry on issue #1564 (comment `aw_run_27`).

## Completed this run (28294779111)

- Created branch `repo-assist/test-mixpay-pre-order-model-2026-06-27` (commit `0b1ab138`) with `test/models/mixpay_pre_order_test.rb`.
- Posted run entry on issue #1564 (comment `aw_run_27`).
- No human issues to fix or comment on. No Dependabot queue action needed.

## Prior runs (compressed)

- **28278300590** (2026-06-27 04:30 UTC): Action model tests (commit `86a93c5c`) → PR #1754/#1753.
- **28253287054** (2026-06-26 17:14 UTC): notifier tests → PR #1746.
- **28237903823** (2026-06-26 12:38 UTC): `HtmlPostProcessor#transform` → PR #1745.
- **28226940061** (2026-06-26 09:31 UTC): `CreateTagService` → PR #1743.
- **28217495965** (2026-06-26 04:53 UTC): `TokenAsset` → PR #1740.
- **28188169668** (2026-06-25 15:30 UTC): No code changes. #1717 closure rec.
- **28116478465** (2026-06-24 17:21 UTC): `joins(:currency)` perf → PR #1731.
- **28098485167** (2026-06-24 12:48 UTC): owner_id typo → #1728 + User#available_articles → #1729.
- **28075390697** (2026-06-24 04:48 UTC): `remote_image_tag` 4→29 files → PR #1730.
- **27987716241** (2026-06-22 23:06 UTC): Dependabot bundle #1717 + `SubdomainConstraint` #1718.
- **27723165917** (2026-06-17 22:50 UTC): `Currency` test stub filled → PR #1682.

## Backlog / future work

- Re-engage when human issues appear. All 9 open issues are auto-generated.
- **Notifier test directory at 100% file coverage** (16 notifiers / 16 test files).
- **`MixpayPreOrder` (tested 2026-06-27)** — STI subclass of `PreOrder`. `:ensure_mixpay_supported` calls `item.mixpay_supported?` only (does NOT consult `quote_assets_cached`). `pay_url` = `https://mixpay.me/pay?payeeId=...&settlementAssetId=...&quoteAssetId=...&quoteAmount=...&traceId=...&settlementMemo=...&returnTo=pre_order_url(follow_id)`.
- **`Action` model coverage (PR #1754/#1753 merged 2026-06-27)** — polymorphic + optional `user`/`target`, `#notifications`, `destroy_notifications` cascade + `rescue StandardError`, `notify_target` dispatch (only `:subscribe + User target` fires).
- **`CollectionListedNotifier` (tested 2026-06-26)**: APP_CARD data exposes icon_url, title (36), description (72), action URL.
- **`PaymentRefundedNotifier` (tested 2026-06-26)**: `t(".refunded", item: ...)`. URL = mixin snapshot URL. PLAIN_TEXT. **`memo:` must be set explicitly on `Payment.new`**.
- **Latent `Currency#store :raw` JSONB bug** — partially addressed by maintainer in `ef58c599` + `66a7fc1d` + `7b6f52a9`. Defer further action.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 (Authorization Boundary Hygiene) and #1694 (Webhook/Callback/Payment-Replay Integrity) are maintainer-led design discussions; out of scope.
- 858b5779 production Solid Cache/Cable/Queue SQLite migration → future SQLITE_BUSY contention check (maintainer-led).
- `Admin::CommentsController#show` removed in PR #1721. Editor layout Twitter handle fixed in PR #1724.
- `modal-component`, `load-more`, `session` Stimulus controllers are used.
- **Bundle #1717 is REDUNDANT** — all four PRs merged individually. Recommend closing.
- **Stale "Currency test coverage" line on #1564** — refers to branch (commit 2e326078) never turned into a PR. Safe to remove on next rewrite.
- **#1564 body** at ~9.5 KB. `update_issue` limit is 10 KB. Continue using `add_comment` until body exceeds 10 KB or maintainer requests a rewrite.

## Notes

- `gh aw compile` produces `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR finalised via `safeoutputs create_pull_request`.
- `bin/rubocop` does NOT lint ERB or YAML. `bin/rails zeitwerk:check` is the local equivalent of the CI load-path check.
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`. "Dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a stub left from a migration.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route AND a view file exists at `app/views/<namespace>/<controller>/<verb>.html.erb`.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])` — copy-paste where the second binding uses the wrong param key.
- **Pre-existing test errors in CI-clean code**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag` helper** (added in `ef58c599`): returns `nil` for nil/blank or non-absolute-URL source, otherwise calls `image_tag`. Translates `lazy: true` to `loading: "lazy"`.
- **`User#available_articles` perf pattern**: push `.uniq` to SQL via `.or.or.distinct`. Verified set-equivalent on `Reader Two` (id 45371440).
- **`includes(:currency).sum(...)` antipattern**: Rails silently falls back to a JOIN when `.sum` references another table's column. Use `joins(:currency)`.
- **`HomeController#active_authors` perf pattern**: `where.not(id: current_user.block_user_ids)` materializes the full block list. perf-improver PR #1735 uses `NOT IN (SELECT ...)` SQL subquery.
- **`setTimeout(fn(), ms)` antipattern**: invoking `fn()` synchronously then scheduling `undefined`. Fixed in `cbd10abd`.
- **Bundle issue lifecycle** (#1717): opened 2026-06-22 bundling #1715 + #1716. Both got superseded. All four bundle PRs merged by `an-lee`. Bundle now redundant.
- **`TokenAsset`** (PORO at `app/models/token_asset.rb`): Balance = `token["balance"].to_r / 10**token["decimals"].to_i` (round 8 dp), or 0 if `token.blank?`. `balance_usd = (balance * currency.price_usd.to_f).round(4)`. Delegates 9 accessors to currency. `attr_reader :contract` declared but `@contract` never assigned — returns `nil`.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory (no callback fires). Do NOT call `Currency#update!` in tests.
- **`User#create_action` returns a boolean** (action_store gem). `Action#after_create :notify_target` is the production hook. `Action` extends `ActiveRecord::Base` directly (not `ApplicationRecord`), polymorphic `target`/`user` with `optional: true`, has_many `:noticed_events` as `:record` (dependent: :destroy), `#notifications` returns `noticed_events`, `#destroy_notifications` cascades (rescues StandardError), `#notify_target` dispatches `SubscribeUserActionCreatedNotifier` only for `action_type == :subscribe && target.is_a?(User)`.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. A non-empty array of blanks is NOT blank, but `compact_blank!` filters the blanks and the body continues.
- **`Tagging#articles_count` counter cache**: declared on `Tagging#belongs_to :tag, counter_cache: :articles_count`.
- **`HtmlPostProcessor#transform` signature**: `transform(css, &block)` — takes a CSS selector and a block; the block receives each matched Nokogiri element and mutates it in place. Returns `self`.
- **`Payment#pre_order` lookup**: `PreOrder.find_by(follow_id: decoded_memo["f"])`.
- **`Transfer#snapshot_id` getter**: reads from the `snapshot` JSON column, not a top-level column. Tests need `Transfer.create!(..., snapshot: { "snapshot_id" => uuid })`.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`. The `recipient:` is positional, the rest are keyword args.
- **`MixpayPreOrder` (tested 2026-06-27)**: STI from `PreOrder` (type `"MixpayPreOrder"`). `:ensure_mixpay_supported` validation calls `item.mixpay_supported?` (only). For `Article` items: `mixpay_supported?` returns true if `free?` OR `asset_id in Mixpay.api.settlement_asset_ids`. For `Collection` items: same shape without the `free?` short-circuit. `pay_url` builds an HTTPS URL on `mixpay.me/pay` with `payeeId` (= `QuillBot.api.client_id`), `settlementAssetId` + `quoteAssetId` (both = `asset_id`), `quoteAmount` (= `amount.to_s`), `traceId`, `settlementMemo`, and `returnTo` (= `pre_order_url(follow_id)`). The `quote_assets_cached` envelope is NOT consulted by the validation; only `PreOrder#mixpay_supported?` reads it.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: in 28294779111 (this run), 28278300590 (action-model), and 28270555934 (efficiency-improver), the tool returned `{success: true, patch: {...}, bundle: {...}}` but no PR appeared on GitHub immediately. **However**: the action-model branch (commit `86a93c5c`) DID land on `main` later as commits `096e76a0` + `546ce939` (PR #1754 / #1753), and the efficiency-improver hot_tags branch landed as PR #1752. So the `safeoutputs` push appears to happen out-of-band — either via the workflow's own post-step or via a separate trigger. The maintainer should pull the patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}` only if no PR appears within a day.