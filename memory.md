---
name: repo-assist-memory
description: Repo Assist run state, completed work, open backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28684209719 on 2026-07-03 21:32 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs**: 3 drafts (all push-blocked by `contents: write` wall — patch + bundle preserved):
  1. `repo-assist/test-mixpay-api-coverage-2026-07-03` (commit `48121fb5`) — Mixpay::API coverage, 15 tests / 30 assertions green locally.
  2. `repo-assist/fix-refund-memo-typo-2026-07-03` (commit `73e8b7dc`) — F9 from #1821, "REDUND"→"REFUND" refund memo + regression test.
  3. `repo-assist/perf-dashboard-transfers-eager-load-2026-07-03` (commit `53435ed7`) — same N+1 family as merged #1802/#1815.
- **Open issues**: 13. All auto-generated by other AI agents (efficiency-improver, perf-improver, repository-quality-improver, test-improver, aw) or my own monthly activity issue (#1789). No human-submitted issues.
- **Open PRs (non-Repo-Assist)**: PR #1822 `Editorial Web3 UI redesign — public pages` (maintainer an-lee, draft). PR #1827 `[docs] unbloat value-net.md` (auto-gen docs workflow, draft).
- **Recent merges** (this run window): PR #1815 articles eager-load MERGED 2026-07-03 02:32:20 UTC; #1814 docs unbloat MERGED 2026-07-03 02:31:48 UTC; #1813 lexxy 0.9.22 MERGED 2026-07-02 22:32:22 UTC.

## This run (28684209719)

- **Selected tasks**: Task 10, Task 4, Task 3, plus Task 11.
- **Task 3 (Issue Fix)**: F9 from #1821 — `Payment#generate_refund_transfer!` had `memo: "REDUND"` instead of `"REFUND"`. One-line fix + regression test. Verified scope: `Grep REDUND` returns exactly one hit; no lookup-key usage. Committed as `73e8b7dc` on `repo-assist/fix-refund-memo-typo-2026-07-03`. Draft PR via safeoutputs. Local: 14 runs / 30 assertions on `test/models/payment_test.rb`; rubocop clean; zeitwerk clean.
- **Task 4**: No-op — Dependabot auto-flowing (4 dep PRs merged in 7 days), CI uses latest actions versions, no build-system gaps. Substituted to Task 10.
- **Task 10**: Re-attempted the perf-improver's push-blocked `Dashboard::TransfersController#index` eager-load (same target as #1823). Single-line change: `.includes(:currency, source: { item: :author })`. Same N+1 family as merged PRs #1802 (collections) and #1815 (articles). Committed as `53435ed7` on `repo-assist/perf-dashboard-transfers-eager-load-2026-07-03`. Draft PR via safeoutputs. Local: rubocop clean, zeitwerk clean. No new test file (matches #1802/#1815 precedent).
- **Task 11**: Updated #1789. Body ~6000 bytes (well under 10 KB cap). Replaced whole body (preserved 2026-07-03 16:56 UTC entry; trimmed older runs).

## Previous run (28673327022) — see #1789 Run History

- Task 9 (Mixpay::API test coverage), Task 3 (no-op), Task 2 (no-op), Task 11.

## Earlier runs — see #1789 Run History

- 28660097062: Task 1/2/3 no-ops, Task 11.
- 28637693543: Task 2/3/5 (deferred).
- 28622852314, 28607591885: Task 3 no-op (auth-name fix already merged as PR #1811), Task 2 no-op, Task 5 deferred, Task 11.
- 28589515383: Task 3 (auth-name fix, merged as PR #1811 same day), Task 2 no-op, Task 10 (articles perf deferred), Task 11.

## Backlog

- **Concern testing (1 remaining)**: `rich_text_content` — blocked on whether maintainer wants the concern split from inline `before_save :track_content_change` callback.
- **Issue #1778** (AGENTS.md codify concern-test convention): CLOSED 2026-07-01 as `not_planned`.
- **Open perf (revival candidate)**: `Dashboard::PaymentsController#index` `:currency` eager-load (branch `b31a4b47`, efficiency-improver agent).
- **Open perf (RESOLVED this run-window)**: `Dashboard::TransfersController#index` — new draft PR `repo-assist/perf-dashboard-transfers-eager-load-2026-07-03` (commit `53435ed7`).
- **Open perf (RESOLVED)**: `Dashboard::ArticlesController#index` — merged as PR #1815 on 2026-07-03.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience.
- #1667 + #1686 + #1694 + #1771 + #1790 + #1794-#1798: maintainer-led design discussions, out of scope.
- **Duplicated perf-improver PRs** (#1783 + #1784): both merged; workflow-determinism issue deferred.
- **Issue #1821** (BigDecimal/DistributeService audit, F1-F12): F9 closed this run. F1/F2/F3 HIGH priority still blocked on maintainer design call + #1571's cooldown.
- **Issue #1717** (bundle graphql+lexxy): 4 Dependabot PRs it referenced were merged individually — supersede with a note (added to Suggested Actions this run).
- **Test gaps** (next-round, lower priority): `MarkdownRenderService`, `RichTextRenderService`, `admin_notification_service`, `text_notification_service` lack direct unit tests.

## Notes

- `bin/rubocop` does NOT lint ERB/YAML. `bin/rails zeitwerk:check` is the local CI load-path check.
- Minitest 6.0.6: `Object#stub` removed — use `define_singleton_method` + `ensure`.
- `safeoutputs update_issue` body limit: 10 KB hard cap. This run's body ~6000 bytes — within budget.
- `safeoutputs push_repo_memory` limit: 12 KB total per push.
- **Concern testing pattern**: one canonical instance per concern (Comment for SoftDeletable, Article for Articles::*, MixinPreOrder for PreOrders::*, Order for Orders::*). Pin decision table for predicates.
- **Test env cache**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore in teardown.
- **AR association re-query on update**: `article.association` re-queries DB and replaces in-memory object. Singleton-method stubs lost.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (SSL to Mixpay). CI stubs them.
- `User#available_articles` perf: push `.uniq` to SQL via `.or.or.distinct` (PR #1735).
- `includes(:currency).sum(...)` antipattern: use `joins(:currency)`. Closed in PR #1737.
- `Currency#save` raises in test env (`QuillBot.api.asset(asset_id)` in `before_validation`). Use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- GitHub MCP `list_pull_requests` defaults to ascending — pass `direction: "desc"`.
- MCP `dependabot alerts`: returns 400 "Pagination using the `page` parameter is not supported". Unreliable.
- **`UserAuthorization` store_accessor quirk (RESOLVED via PR #1811)**: `auth.name` reads `raw["name"]` — but Mixin API populates `full_name`, not `name`. Always use `auth.raw["full_name"]`.
- **ActiveStorage eager-load shape**: `Model.includes(:attached_association, attached_association: :blob)` for `has_one_attached`. Confirmed via PR #1802 and PR #1815.
- **Polymorphic nested preload shape**: `.includes(:currency, source: { item: :author })` for `Transfer → source (Order) → item (Article/Collection) → author`. Confirmed via PR #1802, PR #1815, #1823, this run's commit `53435ed7`.
- **No Postgres in this runner**. `bin/rails test` unreliable locally for DB-touching specs; rely on `bin/rails zeitwerk:check` + `bin/rubocop`. Pure unit tests (`test/libs/`, `test/services/`) DO run locally. Model tests (DB-backed) ALSO run locally — payment_test.rb 14/14 green this run.
- **Maintainer-revival pattern (CONFIRMED via PR #1815)**: when `safeoutputs create_pull_request` returns success but no PR opens, the maintainer can fetch the local bundle and push it manually. Pattern succeeded twice now (#1811 auth-name, #1815 articles eager-load). Orphan branches with local commits + bundles are valuable when the change is small, low-risk, aligned with maintainer's recent work.
- **`mcp__github__list_issues` output too large**: 95 KB+ for 13 issues. Use the saved file path and slice with python.
- **`MCP list_issues` defaults to ALL states**: pass `state: "OPEN"` to filter.
- **`define_singleton_method` block scoping**: when defining on a target instance, the block runs in context of THAT instance. `@recording` would resolve to the API's @recording (nil), not the test's. Always capture value in a local first: `recording = @recording; api.define_singleton_method(:client) { recording }`.
- **`ActiveSupport::Cache::Entry` semantics**: `attr_reader :@expires_in` is the *absolute expires-at timestamp*, not the duration. Use `entry.expires_at - Time.now.to_f` to get the remaining TTL in seconds.
- **`REDUND` memo typo (CLOSED this run)**: F9 from #1821. Fixed in commit `73e8b7dc`. Pure user-visible on-chain label, no lookup-key usage anywhere.
- **No-controller-test perf PR pattern**: PRs #1802 (collections eager-load) and #1815 (articles eager-load) both merged code + commit-message-only — no controller-test file added. Followed the same precedent for `repo-assist/perf-dashboard-transfers-eager-load-2026-07-03`. Regression-guard test shape if needed: `ActionController::TestCase` overriding `render`, signing in via `users(:reader_one)`, asserting one `Currency` SELECT and ≤ N `Article`/`Collection`/`User` SELECTs via `ActiveSupport::Notifications.subscribed`.
- **Linter revert risk**: on 2026-07-03 a system reminder indicated external modification of `app/models/payment.rb` and `test/models/payment_test.rb` between safe-output PR creation and memory write. Committed state on `repo-assist/fix-refund-memo-typo-2026-07-03` (commit `73e8b7dc`) reflects my edits; the working tree at HEAD on `main` is the pre-edit baseline. Always verify `git diff <branch>..main` shows the expected delta before declaring a fix landed.