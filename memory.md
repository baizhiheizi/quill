---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28378823056 on 2026-06-29.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs (6 drafts)**: #1761 (Users::Scopable), #1762 (Articles::ContentPreview), #1764 (email_verified typo fix), #1766 (Users::EmailVerifiable), #1770 (Articles::Purchasable, awaiting merge), this run's `repo-assist/test-orders-distributable-concern-2026-06-29-7a2c9b3e5f8d4a01` (commit `95e3400d`, awaiting PR number from safeoutputs push — patch/bundle saved to `/tmp/gh-aw/aw-repo-assist-test-orders-distributable-concern-2026-06-29-7a2c9b3e5f8d4a01.{patch,bundle}`).
- **Open issues**: 10, all auto-generated by `github-actions[bot]`. 0 human-submitted.
- **Concern testing**: 7 of 12 model concerns now have dedicated tests (this run added Orders::Distributable). **5 remain**: articles/poster_generator, pre_orders/swappable, users/mvm_queriable, authenticatable, rich_text_content.
- **Dependabot queue clear.** Bundle #1717 REDUNDANT — recommend close.
- **#1564 body** ~10 KB; `update_issue` limit is 10 KB. Continue using `add_comment`.

## This run (28378823056)

- **Selected tasks**: Task 3 (Issue Investigation and Fix), Task 5 (Coding Improvements), Task 10 (Take the Repository Forward), plus Task 11.
- **Task 3**: Not applicable. 0 open issues labelled `bug` / `help wanted` / `good first issue`. All 10 open issues are auto-generated by `github-actions[bot]`. Fallback Task 2 also no-op.
- **Task 5 + Task 10 DONE**: Added `test/models/concerns/orders/distributable_test.rb` — 15 tests / 29 assertions / 0 failures / 0 errors. Covers `distribute_async`, `distribute!` delegation, `early_orders` scope correctness (same item, strict `id<? and created_at<?` window, buy/reward orders only, DESC order, excludes self/later), `early_orders_with_the_same_currency` predicate memoization (empty → true, all-same-asset → true, any-foreign → false, memoized), and `collect_early_readers` grouping (groups trace_ids by buyer.mixin_uuid, preserves DESC order per buyer). Branch `repo-assist/test-orders-distributable-concern-2026-06-29-7a2c9b3e5f8d4a01`, commit `95e3400d`. 1 file, +273 lines. Built off main (no PR-stack dependency).
  - **Validation bypass needed**: `Order`'s `uniqueness` validation on `(order_type, buyer_id, item_id, item_type)` for buy_article blocks multiple buys on the same `(buyer, article)` pair. Added a `create_buy_order_raw!` helper that calls `save!(validate: false)` so the early-order branches can be exercised.
  - **Service stub**: For the `distribute!` delegation test, `Orders::DistributeService.call` is monkey-stubbed and restored in `ensure` (no-op), avoiding any side effects on `transfers` / `state`.
  - `bin/rails zeitwerk:check` clean. `bin/rubocop test/models/concerns/orders/distributable_test.rb` clean. `bin/rails test test/models/concerns/orders/distributable_test.rb` 15/29/0/0. `bin/rails test test/models/concerns/` 99/221/0/0 (84 prior + 15 new).
- **Task 11**: Posted run entry comment on #1564 (10KB body too large for `update_issue`).

## Prior runs (compressed)

- **2026-06-29 (run 28363896224)**: #1766 Users::EmailVerifiable tests. Built atop #1764. Run entry on #1564.
- **2026-06-29 (run 28349642487)**: #1766 Users::EmailVerifiable tests. Built atop #1764.
- **2026-06-28**: #1762 Articles::ContentPreview tests. #1761 Users::Scopable tests. #1764 (draft) email_verified typo fix.
- **2026-06-27**: #1758 SoftDeletable. #1756 MixpayPreOrder. #1754/#1753 Action.
- **2026-06-26**: #1746 notifier tests. #1745 HtmlPostProcessor. #1743 CreateTagService. #1740 TokenAsset. #1736 annotaterb bump (Dependabot merge).
- **2026-06-22**: #1717 Dependabot bundle + #1718 SubdomainConstraint.

## Backlog

- Re-engage when human issues appear. All 10 open issues auto-generated.
- Best next concern-test candidates: `pre_orders/swappable` (small surface, 4 methods, but heavy on external API calls — needs QuillBot/PandoLake stubs) or `users/mvm_queriable` / `users/authenticatable` / `users/rich_text_content` / `articles/poster_generator`.
- **Orders::Distributable (tested 2026-06-29)** — used by Order. 5 methods. Tests bypass `Order` uniqueness validation via `create_buy_order_raw!` helper; `Orders::DistributeService.call` is monkey-stubbed for the delegation test.
- **Articles::Purchasable (tested 2026-06-29)** — used by Article. 2 predicates: `may_buy_by?` and `authorized?`. Stubbing `Collection#authorized?` in tests is fragile due to AR re-querying — use real collection + order, or skip the collection branch.
- **Articles::ContentPreview (tested 2026-06-28)** — used by Article. 6 methods. `case child when Nokogiri::XML::NodeSet` is **unreachable** from `each` over `.children`.
- **Users::Scopable (tested 2026-06-28)** — used by User. 13 scopes. Provider-filter scopes only resolve via `joins(:authorization)`.
- **`Users::EmailVerifiable` (typo fixed 2026-06-28; tested 2026-06-29)** — was `email_vefified?` (v-e-f-i-f-i-e-d). DB column `email_verified_at`, scope `only_email_verified`, mailer all correctly spelled `verified`. 3 callers in `_email_field.html.erb` updated.
- **MixpayPreOrder (tested 2026-06-27)** — STI subclass of `PreOrder`. `:ensure_mixpay_supported` calls `item.mixpay_supported?` only.
- **SoftDeletable (tested 2026-06-27)** — used by `Comment` and `AccessToken`. Three scopes, `deleted?` predicate, four actions.
- **Action model (#1754/#1753 merged 2026-06-27)** — polymorphic + optional `user`/`target`.
- **CollectionListedNotifier (tested 2026-06-26)**: APP_CARD data exposes icon_url, title (36), description (72), action URL.
- **PaymentRefundedNotifier (tested 2026-06-26)**: `memo:` must be set explicitly on `Payment.new`.
- **Latent `Currency#store :raw` JSONB bug** — partially addressed by maintainer; defer.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 + #1694 are maintainer-led design discussions; out of scope.
- 858b5779 Solid Cache/Cable/Queue SQLite migration → future SQLITE_BUSY check (maintainer-led).
- `Admin::CommentsController#show` removed in PR #1721.
- **Bundle #1717 REDUNDANT** — recommend closing.
- **Doc issue #1757** (2026-06-27) touches protected `CONTRIBUTING.md`; needs human manual PR.

## Notes

- `bin/rubocop` does NOT lint ERB or YAML. `bin/rails zeitwerk:check` is the local CI load-path check.
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`. "Dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a migration stub.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route AND a view file exists.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])`.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag`** (`ef58c599`): `nil` for nil/blank or non-absolute-URL; otherwise `image_tag`. `lazy: true` → `loading: "lazy"`.
- **`User#available_articles` perf**: push `.uniq` to SQL via `.or.or.distinct`.
- **`includes(:currency).sum(...)` antipattern**: use `joins(:currency)`. **Closed in `Article#author_revenue_usd` / `#reader_revenue_usd`**.
- **`HomeController#active_authors` perf**: PR #1735 uses `NOT IN (SELECT ...)`.
- **`setTimeout(fn(), ms)` antipattern**: invokes `fn()` synchronously. Fixed in `cbd10abd`.
- **`TokenAsset`** (PORO at `app/models/token_asset.rb`): Balance = `token["balance"].to_r / 10**token["decimals"].to_i` (round 8 dp), or 0 if `token.blank?`. Delegates 9 accessors to currency. `attr_reader :contract` declared but `@contract` never assigned — returns `nil`.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. Non-empty array of blanks gets `compact_blank!` cleanup.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: confirmed 2026-06-28 and 2026-06-29. The push happens out-of-band. Maintainer should pull the patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}` only if no PR appears within a day.
- **Concern testing pattern**: write the test through one canonical instance (e.g. `Comment` for `SoftDeletable`); add aliased-scopes instances where needed. Avoid mocking the callback chain.
- **Order uniqueness validation workaround**: `Order`'s `uniqueness` on `(order_type, buyer_id, item_id, item_type)` for buy_article blocks multiple buys on the same `(buyer, article)` pair. Use `save!(validate: false)` via a `create_buy_order_raw!` helper when exercising early-order branches.
- **`test/models/concerns/` directory** has 7 test files (after this run). Per #1744, 5 of 12 model concerns remain untested.
- **`Nokogiri::HTML.fragment` iteration**: `fragment.children` returns `Node` instances (Element, Text, Comment) — NEVER `NodeSet`. Any `case child when Nokogiri::XML::NodeSet` branch in code that iterates `.children` is dead code.
- **Test env cache pattern**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)`, `article.update_columns(association_id: ...)`, and even `article.association` re-queries the DB and replaces the in-memory object. Singleton-method stubs on the original object are lost. Use a real persisted record + the existing helper (`create_buy_order!` for `buy_article`), or skip the branch.
- **Service stub restore pattern**: For tests that pin delegation contracts (e.g. `Order#distribute! → Orders::DistributeService.call`), monkey-stub the service method and restore in `ensure`. Use `Orders::DistributeService.singleton_class.send(:remove_method, :call)` then `define_method(:call) { |order| new(order).call }` to restore the original.