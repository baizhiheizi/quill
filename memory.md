---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28328857302 on 2026-06-28 14:30 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs (2 drafts)**: PR #1761 (Users::Scopable, prior run); this run's `repo-assist/test-articles-content-preview-concern-2026-06-28` (commit `13dd4a42`, patch + bundle at `/tmp/gh-aw/aw-repo-assist-test-articles-content-preview-concern-2026-06-28.{patch,bundle}` 9311/3359 bytes). Other open PR: #1760 (perf-improver Order#notify_subscribers).
- **Open issues**: 9, all auto-generated by `github-actions[bot]`. 0 human-submitted.
- **Dependabot queue clear.** PR #1736 merged 2026-06-26. Bundle #1717 REDUNDANT — recommend close.
- **#1564 body** ~9.5 KB; continued using `add_comment`. Comment `aw_ho8HFwpb` posted.

## This run (28328857302)

- **Task 9 DONE**: `test/models/concerns/articles/content_preview_test.rb` (18 tests / 37 assertions / 0/0/0/0). Pins 6 methods × 3 families: plain-text (`words_count`, `partial_content`, `default_intro`), HTML fragment (`extract_html` full-include / empty-passthrough / Text-truncate / **Element-passthrough**, `partial_content_as_html`), engagement (`upvote_ratio`). Long-content bodies built in-test via `ActionText::RichText` to avoid touching shared fixtures. Branch `repo-assist/test-articles-content-preview-concern-2026-06-28`, commit `13dd4a42`. `test/models/` → 282/651/0/0/1. Rubocop clean. Zeitwerk clean. Opened draft PR via `safeoutputs create_pull_request`.
- **Task 10 DONE (no-op)**: PR #1761 + new content_preview PR in flight. Bundle #1717 still REDUNDANT — recommended close in comment.
- **Task 8 DONE (no-op)**: `Article#author_revenue_usd` / `#reader_revenue_usd` already use `joins(:currency).sum(...)` (lines 215-220). PRs #1759 + #1760 closed HIGH/MEDIUM. Perf-improver backlog empty.
- **Task 11**: posted run entry on #1564 (`aw_ho8HFwpb`).

## Prior runs (compressed)

- **28321469137** (2026-06-28 13:25 UTC): Users::Scopable → PR #1761.
- **28302089997** (2026-06-27): SoftDeletable → PR #1758.
- **28294779111** (2026-06-27): MixpayPreOrder → PR #1756.
- **28278300590** (2026-06-27): Action model → PR #1754/#1753.
- **28253287054** (2026-06-26): notifier tests → PR #1746.
- **28237903823** (2026-06-26): `HtmlPostProcessor#transform` → PR #1745.
- **28226940061** (2026-06-26): `CreateTagService` → PR #1743.
- **28217495965** (2026-06-26): `TokenAsset` → PR #1740.
- **27987716241** (2026-06-22): Dependabot bundle #1717 + `SubdomainConstraint` #1718.

## Backlog

- Re-engage when human issues appear. All 9 open issues auto-generated.
- **Per #1744, 4 of 12 model concerns now have dedicated tests**: SoftDeletable, Users::Statable, Users::Scopable, Articles::ContentPreview. **8 remain untested**: articles/poster_generator, articles/purchasable, pre_orders/swappable, users/email_verifiable, users/mvm_queriable, authenticatable, rich_text_content, orders/distributable. **Best next candidate**: `articles/purchasable` (2 predicates) or `users/email_verifiable` (callback + cache).
- **Articles::ContentPreview (tested 2026-06-28)** — used by Article. 6 methods. `extract_html` `case child when Nokogiri::XML::NodeSet` is **unreachable** from `each` over `.children` (children are Nodes, not NodeSets). Element children are emitted whole; only Text/NodeSet children are truncated.
- **Users::Scopable (tested 2026-06-28)** — used by User. 13 scopes. Provider-filter scopes write `where(authorization: { provider: ... })`, only resolves via `joins(:authorization)`.
- **MixpayPreOrder (tested 2026-06-27)** — STI subclass of `PreOrder`. `:ensure_mixpay_supported` calls `item.mixpay_supported?` only.
- **SoftDeletable (tested 2026-06-27)** — used by `Comment` and `AccessToken`. Three scopes, `deleted?` predicate, four actions. `AccessToken.kept` aliases `without_deleted`.
- **Action model (PR #1754/#1753 merged 2026-06-27)** — polymorphic + optional `user`/`target`, `notify_target` dispatches `SubscribeUserActionCreatedNotifier` only for `action_type == :subscribe && target.is_a?(User)`.
- **CollectionListedNotifier (tested 2026-06-26)**: APP_CARD data exposes icon_url, title (36), description (72), action URL.
- **PaymentRefundedNotifier (tested 2026-06-26)**: URL = mixin snapshot URL. PLAIN_TEXT. **`memo:` must be set explicitly on `Payment.new`**.
- **Latent `Currency#store :raw` JSONB bug** — partially addressed by maintainer; defer.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 + #1694 are maintainer-led design discussions; out of scope.
- 858b5779 Solid Cache/Cable/Queue SQLite migration → future SQLITE_BUSY check (maintainer-led).
- `Admin::CommentsController#show` removed in PR #1721.
- `modal-component`, `load-more`, `session` Stimulus controllers are used.
- **Bundle #1717 REDUNDANT** — recommend closing.
- **New doc issue #1757** (2026-06-27) touches protected `CONTRIBUTING.md`; needs human manual PR.
- **#1564 body** ~9.5 KB; `update_issue` limit is 10 KB. Continue using `add_comment`.

## Notes

- `bin/rubocop` does NOT lint ERB or YAML. `bin/rails zeitwerk:check` is the local CI load-path check.
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`. "Dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a migration stub.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route AND a view file exists.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])` — copy-paste where the second binding uses the wrong param key.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag`** (`ef58c599`): `nil` for nil/blank or non-absolute-URL; otherwise `image_tag`. `lazy: true` → `loading: "lazy"`.
- **`User#available_articles` perf**: push `.uniq` to SQL via `.or.or.distinct`. Verified set-equivalent on `Reader Two` (id 45371440).
- **`includes(:currency).sum(...)` antipattern**: use `joins(:currency)`. **Closed in `Article#author_revenue_usd` / `#reader_revenue_usd`** (lines 215-220).
- **`HomeController#active_authors` perf**: `where.not(id: current_user.block_user_ids)` materializes the block list. PR #1735 uses `NOT IN (SELECT ...)`.
- **`setTimeout(fn(), ms)` antipattern**: invokes `fn()` synchronously then schedules `undefined`. Fixed in `cbd10abd`.
- **Bundle issue lifecycle** (#1717): all four bundle PRs merged by `an-lee`. Bundle now redundant.
- **`TokenAsset`** (PORO at `app/models/token_asset.rb`): Balance = `token["balance"].to_r / 10**token["decimals"].to_i` (round 8 dp), or 0 if `token.blank?`. Delegates 9 accessors to currency. `attr_reader :contract` declared but `@contract` never assigned — returns `nil`.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory.
- **`User#create_action` returns a boolean** (action_store gem). `Action#after_create :notify_target` is the production hook.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. A non-empty array of blanks is NOT blank, but `compact_blank!` filters the blanks and the body continues.
- **`Tagging#articles_count` counter cache**: `Tagging#belongs_to :tag, counter_cache: :articles_count`.
- **`HtmlPostProcessor#transform` signature**: `transform(css, &block)` — block mutates matched Nokogiri element in place. Returns `self`.
- **`Payment#pre_order` lookup**: `PreOrder.find_by(follow_id: decoded_memo["f"])`.
- **`Transfer#snapshot_id` getter**: reads from the `snapshot` JSON column. Tests need `Transfer.create!(..., snapshot: { "snapshot_id" => uuid })`.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`. `recipient:` is positional, the rest are keyword args.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: confirmed in 28321469137, 28328857302 (this run). The push happens out-of-band. Maintainer should pull the patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}` only if no PR appears within a day.
- **Concern testing pattern**: write the test through one canonical instance (e.g. `Comment` for `SoftDeletable`); add aliased-scopes instances where needed (e.g. `AccessToken` where `kept` is an alias for `without_deleted`). Avoid mocking the callback chain.
- **`test/models/concerns/` directory** has 4 test files. Per #1744, 8 of 12 model concerns remain untested.
- **`Nokogiri::HTML.fragment` iteration**: `fragment.children` returns `Node` instances (Element, Text, Comment) — NEVER `NodeSet`. Any `case child when Nokogiri::XML::NodeSet` branch in code that iterates `.children` is dead code.