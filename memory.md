---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28477936491 on 2026-06-30 15:30 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs (3, draft)**: poster_generator (PR #1782, commit `de246db0`) + swappable (PR #1785, commit `4c353f36`) + AGENTS.md concern-test convention (this run, branch `repo-assist/eng-codify-concern-test-convention-2026-06-30`, commit `6c8dbb9f`; PR number not yet visible at end of run, expected ~#1787).
- **Open issues**: 14, all auto-generated by `github-actions[bot]`. 0 human-submitted.
- **Concern testing**: 10 of 12 model concerns have dedicated tests. **2 remain**: `authenticatable` (top-level — 3 OAuth flows), `rich_text_content` (ActionText + Ransacker).
- **AGENTS.md convention codified (this run, closes #1778)**: concern-level test file pattern documented under `## Testing Conventions → ### Concern test files`. Six rules: mirror concern path; one concern/one test file; cover public method surface + documented failure modes; test against a canonical consumer; stub with `define_singleton_method` + `ensure`; hermetic stubs.

## This run (28477936491)

- **Selected tasks**: Task 4 (Engineering Investments), Task 3 (Issue Investigation and Fix), Task 2 (Issue Investigation and Comment), plus Task 11.
- **Task 4**: Codified concern-level test file convention in `AGENTS.md` (32-line `### Concern test files` subsection under `## Testing Conventions`, doc-only). Closes #1778. Branch `repo-assist/eng-codify-concern-test-convention-2026-06-30` (commit `6c8dbb9f`). PR opened (number expected ~#1787). `bin/rails zeitwerk:check` → all is good.
- **Task 3**: Not applicable. 14 open issues all auto-generated by `github-actions[bot]`; no `bug` / `help wanted` / `good first issue` candidates.
- **Task 2**: Not applicable. No human-submitted issues to comment on. PR for #1778 closes the only relevant open issue.
- **Task 11**: Replaced body of `[repo-assist] Monthly Activity 2026-06` (#1564) with this run's entry prepended to Run History. Suggested Actions now lists: (a) **Review PR** AGENTS.md concern-test convention (this run); (b) **Review PR** #1785 (swappable); (c) **Review PR** #1782 (poster_generator); (d) **Close issue #1717** (bundle redundant). Body is well under the 10 KB hard cap.

## Prior runs (compressed)

- **2026-06-30 (run 28443978825)**: `PreOrders::Swappable` concern test (16 tests / 48 assertions). PR #1785 (draft). Concern coverage 10/12.
- **2026-06-30 (run 28431434081)**: `Articles::PosterGenerator` concern test (13 tests / 25 assertions). PR #1782 (draft). Concern coverage 9/12.
- **2026-06-30 (run 28420488265)**: `Users::MVMQueriable` concern test (21 tests). PR #1777 closed unmerged. Branch preserved.
- **2026-06-27 — 2026-06-29**: Orders::Distributable (#1772), Users::EmailVerifiable (#1766), Articles::ContentPreview (#1762), Users::Scopable (#1761), SoftDeletable, MixpayPreOrder, Action model (#1754/#1753), email_verified typo fix (#1764).
- **2026-06-26**: #1746/#1745/#1743/#1740 (notifier + service + token tests).
- **2026-06-22**: #1717 Dependabot bundle + #1718 SubdomainConstraint.

## Backlog

- Re-engage when human issues appear. All 14 open issues auto-generated.
- **Concern testing (2 remaining)**: `authenticatable` (top-level, 3 OAuth flows), `rich_text_content` (ActionText + Ransacker SQL + `track_content_change`).
- **Concerns already tested (10/12)**: SoftDeletable, Statable, EmailVerifiable, Scopable, MVMQueriable, Articles::ContentPreview, Articles::Purchasable, Orders::Distributable, Articles::PosterGenerator, **PreOrders::Swappable**.
- Latent `Currency#store :raw` JSONB bug — partially addressed by maintainer (`7b6f52a9`); defer.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 + #1694 + #1771 are maintainer-led design discussions; out of scope.
- **Bundle #1717 REDUNDANT** — recommend close.
- **Duplicated perf-improver PRs** (#1783 + #1784): same content, different branch slugs. Worth investigating once the maintainer wants to harden the perf-improver workflow.
- **Concern-test convention codified in AGENTS.md** (this run, closes #1778). Future concern PRs without a colocated test file are now review-actionable.

## Notes

- **`bundle outdated` and `dependabot alerts` MCP are NOT reliable signals in this sandbox.**
- **`bin/rubocop` does NOT lint ERB or YAML.** `bin/rails zeitwerk:check` is the local CI load-path check.
- **`bin/rubocop AGENTS.md` produces 217 pre-existing false positives** (markdown-as-Ruby). My addition bumped it to 283. Not a real lint issue.
- **Minitest 6.0.6 quirk**: `Object#stub` removed. Use `define_singleton_method` + `ensure` for class- and instance-method stubs.
- **MVM.stub pattern**: `MVM.scan` memoized at `:@scan`. Stub via `MVM.instance_variable_set(:@scan, fake)` + restore.
- **`Rails.application.credentials` stub**: replace with a tiny Struct whose `#dig` returns the configured value, restore in `ensure`.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: confirmed 2026-06-28..30. Maintainer can pull patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}`.
- **`safeoutputs update_issue` body limit**: 10 KB hard cap.
- **`PandoBot::Lake::PairRoutes` Integer `route_id` quirk**: `route_id` MUST be Integer. Use `route_id: 1` in stubs.
- **Concern testing pattern**: write through one canonical instance (Comment for SoftDeletable, Article for Articles::*, MixinPreOrder for PreOrders::*, Order for Orders::*). Pin the decision table for predicates; avoid mocking the callback chain.
- **Test env cache pattern**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)`, `article.update_columns(association_id: ...)`, and even `article.association` re-queries the DB and replaces the in-memory object. Singleton-method stubs on the original object are lost. Use a real persisted record + the existing helper, or skip the branch.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`User#available_articles` perf**: push `.uniq` to SQL via `.or.or.distinct` (PR #1735).
- **`includes(:currency).sum(...)` antipattern**: use `joins(:currency)`. Closed in PR #1737.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **GitHub MCP `list_pull_requests` defaults to ascending order** — pass `direction: "desc"` to get newest-first.
- **`list_pull_requests` PR-number lag**: when `safeoutputs create_pull_request` returns success, the branch may not appear in `git ls-remote origin` or in `list_pull_requests` for several seconds. Reference new PRs by branch + commit SHA in Monthly Activity summaries if PR number is not yet visible.
- **MCP `dependabot alerts` quirk (2026-06-29)**: returns 400 "Pagination using the `page` parameter is not supported". Unreliable signal.