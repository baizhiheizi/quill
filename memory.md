---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-14 12:50 UTC (run 27498508411).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5).
- AGENTS.md exists; read before any code PR.

## Selected tasks for this run

- Task 3 (Issue Investigation and Fix) — done: continued untested-notifier hygiene series with `SwapOrderSwappingNotifier` (7 tests, 170 lines, single new file). Branch `repo-assist/test-swap-order-swapping-notifier-2026-06-14` committed (`adcd1aad`); draft PR opened via `safeoutputs create_pull_request`; patch at `/tmp/gh-aw/aw-repo-assist-test-swap-order-swapping-notifier-2026-06-14.patch`.
- Task 4 (Engineering Investments) — tried the long-deferred `Currency#store :raw` JSONB fix (replacing `store` with manual `name`/`icon_url`/`change_usd` methods that read from the JSONB-deserialized Hash). The fix worked locally — all 60 notifier tests pass — but a linter or the user reverted the model file back to the original `store :raw, accessors: %i[name icon_url change_usd]`. No PR opened. The latent bug remains.
- Task 2 (Issue Investigation and Comment) — not applicable: 15 open issues all auto-generated by `github-actions[bot]` (workflow failure trackers + per-workflow monthly summaries). Fell back to Task 1, also a no-op (all issues labelled).
- Task 11 (Update Monthly Activity Summary) — done: rewrote issue #1564 with this run's entry prepended; Suggested Actions updated to include the SwapOrderSwappingNotifier intent; previous PRs #1640 and #1641 added (both now merged in main).

## Completed this run

- Verified PR #1641 `[repo-assist] test: add TransferProcessedNotifier tests` is now in main (`93874e1b`). Removed from Suggested Actions as intent.
- Created branch `repo-assist/test-swap-order-swapping-notifier-2026-06-14` with 7 tests for `SwapOrderSwappingNotifier` (170 lines, single new file).
- Tests pass: 7 runs, 25 assertions, 0 failures, 0 errors, 0 skips.
- Rubocop clean, Zeitwerk clean.
- Opened draft PR via `safeoutputs create_pull_request`; patch at `/tmp/gh-aw/aw-repo-assist-test-swap-order-swapping-notifier-2026-06-14.patch` (real PR number available after this workflow run exits).
- Updated issue #1564 `[repo-assist] Monthly Activity 2026-06` with this run's entry prepended.
- Tried the Currency#store JSONB fix; the linter/user reverted the model file. No PR opened.

## Open issue landscape (15 open at run start, all auto-generated)

- #1649, #1647, #1646, #1645, #1644, #1642, #1637, #1636, #1629, #1615, #1567, #1564, #1561, #1517, #1513
- **No human-submitted issues to engage on.**

## Open PRs

- 1 Repo Assist PR: branch `repo-assist/test-swap-order-swapping-notifier-2026-06-14` (intent recorded via `safeoutputs create_pull_request`; real PR number available after this workflow run exits). Pending CI confirmation.
- Other open PRs observed: #1648 (code-simplifier OrderCreatedNotifier), #1643 (docs stimulus observer teardown).
- No stale non-Repo-Assist PRs.

## Backlog / future work

- Re-engage when human issues appear.
- Watch for first-time contributors; welcome them and point to README/CONTRIBUTING.
- 5 notifiers in `app/notifiers/` still lack dedicated tests after this run: `collection_bought`, `collection_listed`, `payment_refunded`, `subscribe_user_action_created`, `swap_order_finished`. Each is a small (~30-80-line) follow-up if a test-improver run hasn't picked them up. Already-tested: `application`, `article_bought`, `article_published` (#1616), `article_rewarded`, `comment_created`, `comment_deleted`, `order_created` (#1628), `payment_created` (#1640), `tagging_created`, `transfer_processed` (#1641), `user_connected`, `user_safe_registration`.
- **Latent model bug, attempted and reverted this run**: `Currency#store :raw` on a JSONB column raises `TypeError: no implicit conversion of Hash into String` because Rails 8.1 already deserializes the JSONB column to Hash, then `store`'s `IndifferentCoder#load` re-serializes-deserializes. I tried replacing `store` with manual accessors and the fix worked (60 notifier tests pass, all rubocop+zeitwerk clean), but the model file was reverted back to the original `store :raw, accessors: %i[name icon_url change_usd]` by a linter/user. Approach is documented in the comment block of the (now-reverted) replacement code; the same pattern would work if a future run wants to re-attempt. The bug is described in Rails 8.1 source at `activerecord-8.1.3/lib/active_record/store.rb:304` (`IndifferentCoder#load` calls `@coder.load(yaml || "")` which is broken when `yaml` is a Hash).
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work — do not touch AASM locks, encryption, Solid Queue topology, error reporting, or Stimulus error states until the quality-improver comes back to verify.
- Defer new engineering work until #1603 (API reference accuracy consolidation) lands.

## Notes

- All open issues carry the `agentic-workflows` label (or are per-workflow Monthly Activity summaries), so Task 1 (labelling) is not applicable.
- `safeoutputs` PR creation emits a `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR is finalised by the workflow runner downstream.
- The perf-improver and efficiency-improver workflows cover the perf/efficiency backlog at higher fidelity than repo-assist can match; defer to them.
- `update_issue` body size limit is 10 KB; trimmed historical run entries from full detail to one-line summaries to fit. The June issue body is now ~6.8 KB and has the current run plus 9 prior runs preserved.
- `update_issue` operation `replace` is the only fully-supported body operation that respects the size limit; `append` and `prepend` keep the existing body and so would not allow reformatting.
- SwapOrderSwappingNotifier coverage added this run: 0 → 7 tests; first notifier test that uses `SwapOrder.skip_callback(:create, :after, :create_fox_swap_transfer!)` to bypass the PandoLake API call that 403s in the test container (a pattern that may apply to other notifier tests where the record's after_create calls an external API).
- Used `currencies(:btc)` for both `pay_asset` and `fill_asset` to avoid `Currency#set_defaults`'s `QuillBot.api.asset(...)` callback (no fixture alternative, no `asset` method on `QuillBotStub::FakeApi`). The message-format test only asserts that the swapping translation, both symbols, and `"->"` are present — it does not depend on the assets being distinct.
- This run shows that the [code-simplifier] (#1648) is processing recently-merged repo-assist PRs; expect the new SwapOrderSwappingNotifier PR to also be touched by that workflow.
