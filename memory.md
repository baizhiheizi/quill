---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28443978825 on 2026-06-30 12:30 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs (2, draft)**: poster_generator (PR #1782, commit `de246db0`) + swappable (PR #1785, commit `16c85530`). Previous run's mvm_queriable PR (#1777) was **closed unmerged** at 2026-06-30 08:43 — branch `origin/repo-assist/test-users-mvm-queriable-concern-2026-06-30-768fec5b90bb8608` (commit `56c75a08`) is preserved.
- **Open issues**: 14, all auto-generated by `github-actions[bot]`. 0 human-submitted.
- **Concern testing**: 10 of 12 model concerns now have dedicated tests. **2 remain**: users/authenticatable (3 OAuth flows), rich_text_content (ActionText + Ransacker).

## This run (28443978825)

- **Selected tasks**: Task 5 (Coding Improvements), Task 3 (Issue Investigation and Fix), Task 10 (Take the Repository Forward), plus Task 11.
- **Task 3**: Not applicable. 14 open issues are all auto-generated by `github-actions[bot]`; no `bug` / `help wanted` / `good first issue` candidates.
- **Task 10**: Folded into Task 5 (Swappable concern coverage was the natural forward step on the maintainer's per-#1744 untested-concerns list).
- **Task 5**: Wrote `test/models/concerns/pre_orders/swappable_test.rb` with 16 tests / 48 assertions covering `pay_url` (4 branches — default → direct; same asset → direct; cross-asset → foxswap with MTG kwargs; no-route → nil + safe_pay_url not invoked), `direct_pay_url` (6-kwarg capture), `foxswap_pay_url` (same-asset nil; no-route nil; otherwise MTG members/threshold/`route[:funds]`), `fswap_mtg_memo` (buy_article → minimum_fill: amount; reward_article → minimum_fill: nil), `pay_amount` (same-asset → amount; no-route → nil; cross-asset → `route[:funds]`), `fswap_route` (cache shape, per-instance memoization via `assert_same`, StandardError rescue → nil). Branch `repo-assist/test-pre-orders-swappable-concern-2026-06-30` (commit `16c85530`). 1 file, 396 insertions. `bin/rails test test/models/concerns/pre_orders/swappable_test.rb` → 16 runs, 48 assertions, 0 failures, 0 errors. `bin/rails test test/models/concerns/` → 115 runs, 269 assertions, 0 failures. `bin/rubocop` → clean. `bin/rails zeitwerk:check` → all is good. `bin/rails test test/models/` → 383 runs, 902 assertions, 0 failures (1 pre-existing skip). Concern coverage now 10/12.
- **Task 11**: Replaced body of `[repo-assist] Monthly Activity 2026-06` (#1564) with this run's entry prepended to Run History (top entry is `2026-06-30 12:30 UTC — run 28443978825`). Suggested Actions now lists: (a) **Review PR** swappable (this run, draft, commit `16c85530`); (b) **Review PR** #1782 (poster_generator); (c) **Close issue #1717** (bundle redundant). Fixed the broken #1778 PR link in the previous edition to point to #1782. Body is well under the 10 KB hard cap.

## Prior runs (compressed)

- **2026-06-30 (run 28431434081)**: Articles::PosterGenerator concern test. PR #1782 opened (draft, commit `de246db0`). Branch `repo-assist/test-articles-poster-generator-concern-2026-06-30` at origin.
- **2026-06-30 (run 28420488265)**: Users::MVMQueriable concern test. PR #1777 opened and **closed unmerged at 08:43 UTC**. Branch preserved at origin (commit `56c75a08`).
- **2026-06-29 (run 28404647438)**: Tasks 3/2/4 no-ops. PRs #1772/#1773/#1774/#1775 merged in window.
- **2026-06-27 — 2026-06-29**: Orders::Distributable (#1772), Users::EmailVerifiable (#1766), Articles::ContentPreview (#1762), Users::Scopable (#1761), SoftDeletable, MixpayPreOrder, Action model (#1754/#1753), email_verified typo fix (#1764).
- **2026-06-26**: #1746/#1745/#1743/#1740 (notifier + service + token tests).
- **2026-06-22**: #1717 Dependabot bundle + #1718 SubdomainConstraint.

## Prior runs (compressed)

- **2026-06-30 (run 28420488265)**: Users::MVMQueriable concern test. PR #1777 opened and **closed unmerged at 08:43 UTC**. Branch preserved at origin (commit `56c75a08`).
- **2026-06-29 (run 28404647438)**: Tasks 3/2/4 no-ops. PRs #1772/#1773/#1774/#1775 merged in window.
- **2026-06-27 — 2026-06-29**: Orders::Distributable (#1772), Users::EmailVerifiable (#1766), Articles::ContentPreview (#1762), Users::Scopable (#1761), SoftDeletable, MixpayPreOrder, Action model (#1754/#1753), email_verified typo fix (#1764).
- **2026-06-26**: #1746/#1745/#1743/#1740 (notifier + service + token tests).
- **2026-06-22**: #1717 Dependabot bundle + #1718 SubdomainConstraint.

## Backlog

- Re-engage when human issues appear. All 14 open issues auto-generated.
- **Concern testing (2 remaining)**: `users/authenticatable` (3 OAuth flows — mixin / fennec / mvm_eth), `rich_text_content` (ActionText + Ransacker SQL + `track_content_change`).
- **PreOrders::Swappable (tested 2026-06-30)** — used by PreOrder/MixinPreOrder. 6 methods. Stubs `PandoBot::Lake::PairRoutes` via Integer-`route_id` cache + `PandoLake.api` singleton-method; `QuillBot.api.safe_pay_url` via `define_singleton_method` + `ensure`. Memoization pinned with `assert_same`.
- **Articles::PosterGenerator (tested 2026-06-30)** — used by Article. 7 methods. Stubs cover/poster via `Struct.new(:attached?, :key)`; credentials via `FakeCredentials` Struct; includes `Rails.application.routes.url_helpers` for `user_article_url`.
- **Concerns already tested (10/12)**: SoftDeletable, Statable, EmailVerifiable, Scopable, MVMQueriable, Articles::ContentPreview, Articles::Purchasable, Orders::Distributable, Articles::PosterGenerator, **PreOrders::Swappable**.
- Latent `Currency#store :raw` JSONB bug — partially addressed by maintainer (`7b6f52a9`); defer.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 + #1694 + #1771 are maintainer-led design discussions; out of scope.
- **Bundle #1717 REDUNDANT** — recommend close.

## Notes

- **`bundle outdated` and `dependabot alerts` MCP are NOT reliable signals in this sandbox.** `bundle outdated` errors on read-only filesystem; `dependabot alerts` errors on pagination param.
- **`bin/rubocop` does NOT lint ERB or YAML.** `bin/rails zeitwerk:check` is the local CI load-path check.
- **Minitest 6.0.6 quirk**: `Object#stub` was removed. Use `define_singleton_method` + `ensure` for class- and instance-method stubs.
- **MVM.stub pattern**: `MVM.scan` is memoized at instance variable `:@scan`. Stub via `MVM.instance_variable_set(:@scan, fake)` + restore in `ensure`.
- **`Rails.application.routes.url_helpers`**: include in test class to call route helpers like `user_article_url`.
- **`Rails.application.credentials` stub**: replace with a tiny Struct whose `#dig` returns the configured value, restore in `ensure`.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: confirmed 2026-06-28, 2026-06-29, 2026-06-30. The push happens out-of-band. Maintainer can pull patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}`.
- **`safeoutputs update_issue` body limit**: 10 KB hard cap. Compress prior-run entries aggressively.
- **`PandoBot::Lake::PairRoutes` Integer `route_id` quirk**: `route_id` MUST be an Integer. `Hashids.new.encode` raises `ArgumentError: invalid value for Integer()` on non-integers. Use `route_id: 1` in stubs.
- **`PreOrders::Swappable` test pattern**: pre-populate `Rails.cache["pando_lake_routes"]` (5s expiry) with one BTC↔ETH pair; pair shape: `{base_asset_id, quote_asset_id, base_amount, quote_amount, fee_percent, swap_method, route_id: Integer}`. Stub `PandoLake.api` with the same pair shape so the cache-miss path stays exercisable. `fswap_route` calls `PandoBot::Lake::PairRoutes.new(pairs).pre_order(input_asset:, output_asset:, output_amount: (amount * 1.001).ceil(8).to_f)`. The route hash keys are `:funds`, `:routes`, `:pay_asset_id`, `:fill_asset_id`, `:pay_amount`, `:fill_amount`. `funds` is a formatted `%.8f` String, not a Numeric — assert against the actual returned value, not a hardcoded literal.
- **`MixinPreOrder.create!` validations**: requires `currency` association via `belongs_to :currency, primary_key: :asset_id, foreign_key: :asset_id`. To exercise the cross-asset path without a fixture, build the order with the article's asset_id then `define_singleton_method(:asset_id) { ... }` on the instance — don't rebuild with a foreign asset_id (re-validates and fails).
- **Concern testing pattern**: write through one canonical instance (Comment for SoftDeletable, Article for Articles::*, MixinPreOrder for PreOrders::*, Order for Orders::*). Pin the decision table for predicates; avoid mocking the callback chain.
- **`Order` uniqueness validation workaround**: `save!(validate: false)` via `create_buy_order_raw!` helper when exercising early-order branches.
- **`Nokogiri::HTML.fragment` iteration**: `fragment.children` returns `Node` instances — NEVER `NodeSet`. Any `case child when Nokogiri::XML::NodeSet` branch over `.children` is dead code.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag`** (`ef58c599`): `nil` for nil/blank or non-absolute-URL; otherwise `image_tag`. `lazy: true` → `loading: "lazy"`.
- **`User#available_articles` perf**: push `.uniq` to SQL via `.or.or.distinct` (PR #1735).
- **`includes(:currency).sum(...)` antipattern**: use `joins(:currency)`. Closed in `Article#author_revenue_usd` / `#reader_revenue_usd` (PR #1737).
- **`TokenAsset`** (PORO at `app/models/token_asset.rb`): delegates 9 accessors to currency.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. Non-empty array of blanks gets `compact_blank!` cleanup.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`.
- **Test env cache pattern**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)` and `update_columns(association_id: ...)` re-queries and replaces in-memory. Singleton-method stubs are lost.
- **Test env cache pattern**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)`, `article.update_columns(association_id: ...)`, and even `article.association` re-queries the DB and replaces the in-memory object. Singleton-method stubs on the original object are lost. Use a real persisted record + the existing helper (`create_buy_order!` for `buy_article`), or skip the branch.
- **Service stub restore pattern**: For tests that pin delegation contracts (e.g. `Order#distribute! → Orders::DistributeService.call`), monkey-stub the service method and restore in `ensure`. Use `Orders::DistributeService.singleton_class.send(:remove_method, :call)` then `define_method(:call) { |order| new(order).call }` to restore the original.
- **MCP `dependabot alerts` quirk (2026-06-29)**: returns 400 with message "Pagination using the `page` parameter is not supported" even on first call with no `page` param. Unreliable signal for repo-assist runs.
- **Minitest 6.0.6 quirk (2026-06-30)**: `Object#stub` was removed. Use `define_singleton_method` + `ensure` for class-method stubs (e.g. `Currency.stub(:all, [btc, eth]) { ... }` → `Currency.define_singleton_method(:all) { [btc, eth] }` + restore). For instance-method stubs, also use `define_singleton_method`.
- **MVM.stub pattern (2026-06-30)**: `MVM.scan` is a memoized singleton, so stub via `MVM.instance_variable_set(:@scan, fake)` + restore in `ensure`. Do **not** redefine `MVM.scan` directly because the gem uses `def self.scan; @scan ||= MVM::Scan.new; end`.
- **`safeoutputs update_issue` body limit**: 10 KB hard cap. Compress prior-run entries in the Monthly Activity summary aggressively; one line per task outcome plus the Selected tasks line.