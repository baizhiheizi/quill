- [Repo Assist Memory](memory.md) — run state, completed work, open backlog, monthly issue, and notes
 backlog, monthly issue, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run 28622852314 on 2026-07-02 21:32 UTC.** Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists.
- **Open Repo Assist PRs**: NONE. Auth-name fix from previous run landed as **PR #1811 (MERGED 2026-07-02 13:18:10)** — maintainer revived the local commit `4c6f2493` as branch `repo-assist/fix-auth-name-existing-user-2026-07-02-63d20574f98a2546`. PR #1812 was a duplicate retry (same content, different branch slug), closed unmerged 2026-07-02 13:19:30.
- **Open issues**: 7 (all auto-generated by `github-actions[bot]` or my own monthly activity issue). PR #1813 open (dependabot lexxy bump 0.9.21→0.9.22). PR #1814 open (Documentation Unbloat draft, `docs/unbloat-local-development-4f4fa1a354dc53eb`).
- **Concern testing**: 11/12 covered. 1 remains: `rich_text_content`.
- **Prior `repo-assist` PRs (merged since last run)**: #1811 (auth-name fix, from branch `repo-assist/fix-auth-name-existing-user-2026-07-02-63d20574f98a2546` — maintainer-revived from local commit `4c6f2493`). Previous: #1802 (perf-collections eager-load).
- **`safeoutputs create_pull_request` push failure**: STILL active. PR #1811 was opened by the bot, but the actual push to `repo-assist/fix-auth-name-existing-user-2026-07-02-63d20574f98a2546` was performed by the maintainer from the local commit (the SHA differs from the bot's local commit). Until runner gains push permissions, do not create new orphan branches.

## This run (28622852314)

- **Selected tasks**: Task 3, Task 8, Task 2, plus Task 11.
- **Task 3**: Skipped — `bug` / `help wanted` / `good first issue` search returned 0 open issues. Substituted to Task 2 per fallback rules.
- **Task 8**: Skipped — performance opportunity still identified (`Dashboard::ArticlesController#index` N+1, same shape as PR #1802), but push-blocked pattern unchanged since last run. PR #1811 had to be manually revived from local commit `4c6f2493`; PR #1787 was rejected by the same wall. No new branch created this run.
- **Task 2**: Skipped — all 7 open issues are auto-generated (`github-actions[bot]`) or my own monthly/detection issues (#1789, #1810). No human-submitted issues need engagement. Substituted as no-op.
- **Task 11**: Updated #1789. Body hit the 10 KB cap on first attempt (10406 bytes) — had to drop the two oldest Run History entries (2026-07-01 09:39 UTC and 2026-07-01 04:58 UTC) and added a "see repo-memory for the full history" pointer. Format-spec paragraph (the **Comprehensive list** intro under Suggested Actions) included per spec. Added two new Suggested Actions: review PR #1814 (Documentation Unbloat draft, `docs/local-development.md` tightening) and merge PR #1813 (dependabot lexxy 0.9.21→0.9.22). Future Work items preserved. Prepended the new run entry.

## Previous run (28607591885) — see #1789 Run History

- Task 3 (no-op — auth-name fix already merged as PR #1811), Task 2 (no-op — auto-gen only), Task 5 (deferred articles perf), Task 11.

## Earlier run (28589515383) — see #1789 Run History

- Task 3 (auth-name fix, branch `repo-assist/fix-auth-name-existing-user-2026-07-02` commit `4c6f2493`), Task 2 (no-op), Task 10 (articles perf deferred), Task 11. The auth-name fix landed as merged PR #1811 later that day (2026-07-02 13:18:10).

## Backlog

- **Concern testing (1 remaining)**: `rich_text_content` — blocked on whether maintainer wants `RichTextContent` concern split from inline `before_save :track_content_change` callback.
- **Issue #1778** (AGENTS.md codify concern-test convention): still OPEN. PR #1787 push rejected 2026-06-30. Convention enforced in PR review memory only.
- **Open perf**: `Dashboard::ArticlesController#index` for `published` + `hidden` tabs — same N+1 class as PR #1802. Deferred until push limitation fixed.
- **Open perf (lower priority)**: `Dashboard::TransfersController#index` — polymorphic disambiguation prevents one-line fix. Branch `repo-assist/perf-transfers-dashboard-eager-load-2026-07-01` (commit `f6ea29cf`) preserved locally.
- **Open perf (moot)**: `Dashboard::SwapOrdersController#index` — SwapOrder removed in Phase 0 (#1794).
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience.
- #1667 + #1686 + #1694 + #1771 + #1790 + #1794-#1798: maintainer-led design discussions, out of scope.
- **Duplicated perf-improver PRs** (#1783 + #1784): both merged; workflow-determinism issue deferred.

## Notes

- `bin/rubocop` does NOT lint ERB/YAML. `bin/rails zeitwerk:check` is the local CI load-path check.
- Minitest 6.0.6: `Object#stub` removed — use `define_singleton_method` + `ensure`.
- `Rails.application.credentials` stub: tiny Struct whose `#dig` returns the configured value, restore in `ensure`.
- `safeoutputs update_issue` body limit: 10 KB hard cap. This run's body was ~7.9 KB — within budget.
- `PandoBot::Lake::PairRoutes`: `route_id` MUST be Integer. Use `route_id: 1` in stubs.
- **Concern testing pattern**: one canonical instance per concern (Comment for SoftDeletable, Article for Articles::*, MixinPreOrder for PreOrders::*, Order for Orders::*). Pin decision table for predicates; avoid mocking the callback chain.
- **Test env cache**: `Rails.cache = ActiveSupport::Cache::MemoryStore.new` in setup, restore `@previous_cache` in teardown.
- **AR association re-query on update**: `article.update!(association: x)`, `update_columns(association_id: ...)`, even `article.association` re-queries DB and replaces in-memory object. Singleton-method stubs lost.
- **Pre-existing CI-clean test errors**: `test/controllers/articles_controller_test.rb` (asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (SSL to Mixpay). CI stubs them.
- `User#available_articles` perf: push `.uniq` to SQL via `.or.or.distinct` (PR #1735).
- `includes(:currency).sum(...)` antipattern: use `joins(:currency)`. Closed in PR #1737.
- `Currency#save` raises in test env (`QuillBot.api.asset(asset_id)` in `before_validation`). Use `Currency.new(price_usd: ...)` in memory.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- GitHub MCP `list_pull_requests` defaults to ascending — pass `direction: "desc"`.
- MCP `dependabot alerts`: returns 400 "Pagination using the `page` parameter is not supported". Unreliable.
- **`UserAuthorization` store_accessor quirk (RESOLVED via PR #1811)**: `auth.name` reads `raw["name"]` — but Mixin API populates `full_name`, not `name`. Always use `auth.raw["full_name"]`. Fix landed in PR #1811.
- **ActiveStorage eager-load shape**: `Model.includes(:attached_association, attached_association: :blob)` for `has_one_attached`. Confirmed via `Collection#cover` (PR #1802).
- **No Postgres in this runner**. `bin/rails test` unreproducible locally for DB-touching specs; rely on `bin/rails zeitwerk:check` + `bin/rubocop`, defer to CI.
- **PR push is still blocked**: even when `safeoutputs create_pull_request` returns `success`, the runner cannot push the branch. The maintainer revived PR #1811 manually from the local commit (`2146d452389ff7b3cc5462021d4a1c3f806bbc0d` is the merge SHA, but the bot's local commit was `4c6f2493` — different). Until this is fixed, do not create new branches expecting them to be pushed.
- **`safeoutputs update_issue` body cap**: 10 KB hard limit. First attempt on #1789 hit 10406 bytes and was rejected. Workaround: drop the oldest Run History entries (the spec doesn't mandate retention) and add a one-line pointer to repo-memory. Format-spec paragraph under Suggested Actions takes ~600 bytes; don't drop it — spec mandates it.