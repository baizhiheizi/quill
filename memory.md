---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-23 16:30 UTC (run 28043998628).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5).
- AGENTS.md exists; read before any code PR.
- **Open PRs at run start**: 6 — Dependabot #1715 (graphql 2.6.3→2.6.4), #1716 (lexxy 0.9.18→0.9.19), #1722 (cgi 0.5.1→0.5.2), #1723 (lexxy 0.9.18→0.9.20); Repo Assist #1718 (SubdomainConstraint removal), #1721 (CommentsController#show dead action); efficiency-improver #1719 (prefers-reduced-motion).
- **Open issues at run start**: 8 — all auto-generated by `github-actions[bot]`. 0 human-submitted open issues. #1717 is the failed bundle Dependabot PR (protected files: Gemfile/Gemfile.lock) — created as an issue with patch + bundle artifacts.

## Selected tasks for run 28043998628 (2026-06-23 16:30 UTC)

- **Task 5 (Coding Improvements)** — `app/views/layouts/editor.html.erb` (used by `ArticlesController#new`/`#edit`) hardcoded `@prsdigg` in its Twitter Card `twitter:site` and `twitter:creator` meta tags, while the other two layouts (`application.html.erb`, `homepage.html.erb`) used `@<%= Settings.twitter_account %>` (default `with_quill`). Brand-consistency cleanup; no semantic change in production. Branch `repo-assist/improve-editor-twitter-account-2026-06-23` (commit `a47ea47f`) — 1 file, 2+/2-. Rubocop clean (488 files), zeitwerk clean.
- **Task 10 (Take Repo Forward)** — folded into Task 5. The editor-layout Twitter handle is the natural next brand-consistency step after the closed-out settings inconsistency cycle (PRs #1700, #1698, #1705).
- **Task 2 (Issue Investigation and Comment)** — not applicable. 0 human-submitted issues. Fallback Task 1 also not applicable (0 unlabelled at run start).
- **Task 11** — posted run entry on issue #1564 (temporary_id `aw_7ZDEuJP4`).

## Completed this run (28043998628)

- Opened draft PR `repo-assist/improve-editor-twitter-account-2026-06-23` (commit `a47ea47f`) — uses `Settings.twitter_account` in editor layout's Twitter Card meta tags.
- Posted run entry on issue #1564 (temporary_id `aw_7ZDEuJP4`).

## Prior runs (compressed)

- **27987716241** (2026-06-22 23:06 UTC): Tasks 4, 5 — bundled Dependabot PRs (#1715+#1716, branch `repo-assist/eng-bundle-deps-2026-06-22` → became issue #1717 due to protected Gemfile/Gemfile.lock) and removed unused `SubdomainConstraint` class (PR #1718).
- **27931670396** (2026-06-22): No-op run.
- **27918345901** (2026-06-21 21:44 UTC): Opened `repo-assist/drop-empty-mixpay-button-controller-2026-06-21` — drops 7-line no-op Stimulus stub from PR #1490. Posted run entry.
- **27911156543** (2026-06-21 16:58 UTC): Opened `repo-assist/fix-preview-upload-audio-fallback` (PR #1711).

## Open issue landscape

- #1720 ([efficiency-improver] Proposal), #1717 ([repo-assist] build(deps) bundle — protected-files issue), #1636 ([aw] Detection Runs), #1567 ([aw] No-Op Runs), #1564 ([repo-assist] Monthly Activity 2026-06), #1561 ([efficiency-improver] Monthly Activity 2026-06), #1517 ([test-improver] Monthly Activity 2026-06), #1513 ([perf-improver] Monthly Activity 2026-06).
- **No human-submitted issues to engage on.**

## Open PRs

- 4 Dependabot (#1715 graphql 2.6.4, #1716 lexxy 0.9.19, #1722 cgi 0.5.2, #1723 lexxy 0.9.20) + Repo Assist #1718 (SubdomainConstraint removal) + #1721 (CommentsController#show dead action) + this run's draft editor-twitter-account PR + efficiency-improver #1719.

## Backlog / future work

- Re-engage when human issues appear. Welcome first-time contributors, point to README/CONTRIBUTING.
- 3 notifiers lack dedicated tests: `collection_listed`, `payment_refunded`, `swap_order_finished`. Test-improver owns.
- **Latent `Currency#store :raw` JSONB bug** — `currency.name` / `currency.icon_url` raise `TypeError`. Maintainer has not actioned prior attempts; not re-attempting.
- Settings inconsistency fully resolved (PRs #1698, #1700, #1705) AND editor layout brand consistency now resolved (this run).
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- Specialized workflows (test-improver, efficiency-improver, perf-improver) own the natural notifier-testing/efficiency/perf work.
- #1667 + #1686 (Authorization Boundary Hygiene) and #1694 (Webhook/Callback/Payment-Replay Integrity) are maintainer-led design discussions; out of scope.
- 858b5779 production Solid Cache/Cable/Queue SQLite migration opens future work to verify there's no SQLITE_BUSY contention (maintainer-led).
- **`Admin::CommentsController#show` was unreachable** since at least the route draw was last restructured — `resources :comments, only: %i[index]` does not expose it. Confirmed by absence of view, absence of `admin_comment_path` references, and `params[:uuid]` (which no route supplies). Removed in PR #1721 (open draft, awaiting maintainer).
- **Editor layout Twitter handle**: `app/views/layouts/editor.html.erb` hardcoded `@prsdigg` while the other two layouts used `Settings.twitter_account` — fixed in this run's draft PR.
- **`modal-component` identifier is intentional**: `app/javascript/controllers/index.js:62` registers `FlyonuiModalController` under `modal-component` (not `flyonui-modal`). NOT dead.
- **All Stimulus controllers in `index.js` are used.** No further dead Stimulus code detected this run. (Verified: nested-form, prefetch, references-select, tags-select, collections-form-component, search, turbo all referenced in views via `data-controller=` or `controller:` Rails hash.)
- **Bundling Dependabot PRs is straightforward in principle** but Gemfile/Gemfile.lock are protected from automation pushes. Workaround for future runs: produce patch+bundle artefacts (issue body) and let a maintainer apply manually.

## Notes

- `gh aw compile` produces `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR finalised by workflow runner via `safeoutputs create_pull_request`. PR number assigned by workflow runner.
- `update_issue` body size limit is 10 KB; queued body updates from `schedule`/`workflow_dispatch` are unreliable. Monthly Activity update via `add_comment`.
- `bin/rubocop` and `bin/rails zeitwerk:check` run locally. RuboCop does NOT lint ERB or YAML. CI uses the same config.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- `User#create_action` returns a boolean, not the `Action` record. To get an Action in tests: `Action.create!(action_type:, user:, target:, user_type: "User", target_type: "User")`. `Action#after_create :notify_target` is the production hook.
- Noticed 3 with `record: <model>` fails with `NoMethodError: undefined method 'has_query_constraints?'` if `record` is not a real ActiveRecord instance.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`; tests assert against this value (the `Config::Options` class does not support mocha's `.stubs`).
- `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID` (`d4444444-4444-4444-8444-444444444444`).
- Stimulus dead-code detection: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`, `element.dataset["controller"] = "xxx"`. Use `re.findall(r'data-controller=["\']([^"\']+)["\']', content)`. An identifier is "dead" only if (a) no view references it AND (b) the controller file does not exist. If the controller file exists but is mapped to a different identifier in `index.js`, the original identifier is intentional.
- "Empty controller stub" detection: a Stimulus controller with only a `connect()` that does `console.log(...)` is a stub left from a migration. Check git log; delete the controller, the wrapping div, the `import`/`register` lines, and the catalog row.
- `bin/ci` uses `ActiveSupport::ContinuousIntegration` with: `Setup`, `Style: Ruby` (rubocop), `Style: JavaScript` (bun lint-check), `Tests: Rails`, `Tests: Seeds`.
- `Action` extends `ActiveRecord::Base` directly (not `ApplicationRecord`), polymorphic `target`/`user` with `optional: true`, has `after_create :notify_target` + `before_destroy :destroy_notifications`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring routes. The `SubdomainConstraint` class was dead since Oct 2022 (removed by PR #1718).
- For JS-only changes, the local-toolchain equivalent of `bun run lint-check` is `node_modules/.bin/prettier --check app/javascript/<changed-file>`; `node esbuild.config.js` produces the same bundle CI builds (one pre-existing `DEP0180` deprecation warning).
- GitHub's "unstable" mergeable_state for draft PRs is often transient — use `git merge-tree <merge-base> <main> <branch>` locally to confirm there are no real conflicts.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route declaration AND a corresponding view file exists at `app/views/<namespace>/<controller>/<verb>.html.erb`. If both fail, the action is dead. Cross-check by grepping `path:`, `_path`, and `_url` helpers in `app/views/` and `app/helpers/`.
- **Stale Ruby idiom**: `params[:foo]&.present?` can be `params[:foo].present?` if the route never receives `nil` for that key; but only refactor when the surrounding pattern (e.g. `before_action`) is also being added.
- **Protected files for Dependabot bundling**: Gemfile, Gemfile.lock, and likely package.json/bun.lock are write-protected for `github-actions[bot]`. PRs that touch only those files cannot be auto-pushed via `safeoutputs create_pull_request`. The patch + bundle artefact will be emitted, but the runner will reject the push. Surface this to the maintainer via the issue body and the Monthly Activity Summary.
- **`test/controllers/articles_controller_test.rb` has 3 pre-existing errors** caused by `ActionView::Template::Error: The asset 'application.css' was not found in the load path.` This is a test-env asset-pipeline issue, not related to my changes (verified on `main`). CI has the asset and does not see these errors.