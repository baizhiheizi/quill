---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-26 17:14 UTC (run 28253287054).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists; read before any code PR.
- **Open PRs from Repo Assist**: 4 unmerged draft PRs — TokenAsset tests (#1740, 2026-06-26 04:53 UTC), CreateTagService tests (#1743, 2026-06-26 09:31 UTC), HtmlPostProcessor refactor (#1745, 2026-06-26 12:38 UTC), and notifier tests for `collection_listed` + `payment_refunded` (this run, branch `repo-assist/test-collection-listed-and-payment-refunded-notifiers-2026-06-26`, commit d48e9f53, PR #1746).
- **Open issues**: 9 — all auto-generated by `github-actions[bot]`. 0 human-submitted open issues.
- **Dependabot queue**: clear. PR #1736 (annotaterb 4.22.0→4.23.0) still open. All bundle PRs (#1722 cgi, #1723 lexxy, #1730 mini_racer, #1736 annotaterb) merged individually by `an-lee`. Bundle issue #1717 is now redundant — recommended closure in run 28188169668.

## Selected tasks for run 28253287054 (2026-06-26 17:14 UTC)

- **Task 6 (Maintain Repo Assist PRs)** — DONE. Verified the 3 open Repo Assist PRs (#1745, #1743, #1740) have green GitGuardian Security Checks.
- **Task 10 (Take the Repository Forward)** — DONE. Natural forward step was the same as Task 9 (2 missing notifier test files). Both tasks converged on the same PR.
- **Task 9 (Testing Improvements)** — DONE. Added test coverage for the last 2 notifier files without dedicated tests: `test/notifiers/collection_listed_notifier_test.rb` (14 tests / 30 assertions) and `test/notifiers/payment_refunded_notifier_test.rb` (9 tests / 21 assertions). Branch `repo-assist/test-collection-listed-and-payment-refunded-notifiers-2026-06-26`, commit `d48e9f53`, 2 files, +489/-0. `bundle exec rails test test/notifiers/` → 125 runs, 365 assertions, 0 failures, 0 errors, 1 pre-existing skip. `bundle exec rubocop` → 2 files inspected, no offenses. `bundle exec rails zeitwerk:check` → all is good. Draft PR #1746 opened via `safeoutputs create_pull_request`.
- **Task 11** — posted run entry on issue #1564 (comment `aw_IYMqiMBN`) covering all 3 selected tasks plus the 4 Repo Assist PRs now awaiting review.

## Completed this run (28253287054)

- Created branch `repo-assist/test-collection-listed-and-payment-refunded-notifiers-2026-06-26` (commit `d48e9f53`) with the 2 new notifier test files. Opened draft PR #1746.
- Posted run entry on issue #1564 (comment `aw_IYMqiMBN`).
- No human issues to fix or comment on. No Dependabot queue action needed.

## Prior runs (compressed)

- **28237903823** (2026-06-26 12:38 UTC): Task 5 — `HtmlPostProcessor#transform` extraction. PR #1745 opened (draft).
- **28226940061** (2026-06-26 09:31 UTC): Task 9 — `CreateTagService` test coverage. PR #1743 opened (draft).
- **28217495965** (2026-06-26 04:53 UTC): Task 9 — `TokenAsset` test coverage. PR #1740 opened (draft).
- **28188169668** (2026-06-25 15:30 UTC): No code changes. Status comment on #1717 (bundle) recommending closure.
- **28116478465** (2026-06-24 17:21 UTC): Task 8 — `joins(:currency)` in `Article#author_revenue_usd` / `#reader_revenue_usd`. PR #1731 merged.
- **28098485167** (2026-06-24 12:48 UTC): Task 5 (owner_id typo fix → PR #1728) + Task 8 (User#available_articles SQL refactor → PR #1729). Both merged 2026-06-25.
- **28075390697** (2026-06-24 04:48 UTC): Task 5 — extended `remote_image_tag` from 4 view files to 29. PR #1730 merged.
- **27987716241** (2026-06-22 23:06 UTC): Tasks 4, 5 — bundled Dependabot PRs into #1717 + removed `SubdomainConstraint` (PR #1718).
- **27723165917** (2026-06-17 22:50 UTC): Task 9 — empty `Currency` test stub filled. PR #1682.
- **27694493149** (2026-06-17 15:15 UTC): Tasks 9, 5 — cable connection tests (PR #1679) + dashboard block-users cleanup (PR #1680). Both merged.

## Backlog / future work

- Re-engage when human issues appear. All 9 open issues are auto-generated.
- **Notifier test directory is now at 100% file coverage** (16 notifiers in `app/notifiers/`, 16 test files in `test/notifiers/`).
- **`CollectionListedNotifier` (tested 2026-06-26)**: message joins author name (truncated to 10) + listed translation + ":" + collection name. APP_CARD data payload exposes icon_url (author avatar), title (truncated to 36), description (truncated to 72), and action (collection URL). Web visibility tied to `article_published_web`; mixin bot tied to `article_published_mixin_bot`.
- **`PaymentRefundedNotifier` (tested 2026-06-26)**: message = `t(".refunded", item: payment.pre_order&.item&.title)`. URL = `"https://mixin.one/snapshots/#{payment.refund_transfer.snapshot_id}"`. data = message. PLAIN_TEXT (no APP_CARD). `may_notify_via_mixin_bot?` true for messengers. **`memo:` must be set explicitly on `Payment.new`** (not just in `raw:`) — the model's `before_validation :setup_attributes` reads `raw['memo']` but the column gets reset to nil after a no-validate save.
- **Latent `Currency#store :raw` JSONB bug** — confirmed reproducible. Partially addressed by maintainer in `ef58c599` + `66a7fc1d` + `7b6f52a9`. Defer further action.
- **Maintainer signal**: view patterns started but not finished — keep looking (e.g. `remote_image_tag` was only applied to 4 view files before being extended to 29; same for `lazy: true`).
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- Specialized workflows (test-improver, efficiency-improver, perf-improver, code-simplifier) own notifier-testing / efficiency / perf / simplification work.
- #1667 + #1686 (Authorization Boundary Hygiene) and #1694 (Webhook/Callback/Payment-Replay Integrity) are maintainer-led design discussions; out of scope.
- 858b5779 production Solid Cache/Cable/Queue SQLite migration opens future work to verify no SQLITE_BUSY contention (maintainer-led).
- `Admin::CommentsController#show` removed in PR #1721. Editor layout Twitter handle fixed in PR #1724.
- `modal-component`, `load-more`, `session` Stimulus controllers are used (verified 2026-06-25).
- **Bundle #1717 is REDUNDANT** — all four PRs it referenced were merged individually. Recommend closing.
- **Stale "Currency test coverage" Suggested Actions line on #1564** — refers to a branch (commit 2e326078) that was never turned into a PR. Safe to remove on next rewrite.
- **#1564 body** at ~9.5 KB. `update_issue` limit is 10 KB. Continue using `add_comment` until body exceeds 10 KB or maintainer requests a rewrite.

## Notes

- `gh aw compile` produces `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR finalised via `safeoutputs create_pull_request`.
- `bin/rubocop` does NOT lint ERB or YAML. `bin/rails zeitwerk:check` is the local equivalent of the CI load-path check.
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`. An identifier is "dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a stub left from a migration.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route AND a view file exists at `app/views/<namespace>/<controller>/<verb>.html.erb`.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])` — copy-paste where the second binding uses the wrong param key.
- **Pre-existing test errors in CI-clean code**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag` helper** (added in `ef58c599`): returns `nil` for nil/blank or non-absolute-URL source, otherwise calls `image_tag`. Translates `lazy: true` to `loading: "lazy"`. Use everywhere `.icon_url` is rendered.
- **`User#available_articles` perf pattern**: push `.uniq` to SQL via `.or.or.distinct`. Verified set-equivalent on `Reader Two` (id 45371440).
- **`includes(:currency).sum(...)` antipattern**: Rails silently falls back to a JOIN when `.sum` references another table's column. Use `joins(:currency)`.
- **`HomeController#active_authors` perf pattern**: `where.not(id: current_user.block_user_ids)` materializes the full block list. perf-improver PR #1735 uses `NOT IN (SELECT ...)` SQL subquery.
- **`setTimeout(fn(), ms)` antipattern**: invoking `fn()` synchronously then scheduling `undefined`. Fixed in `cbd10abd`.
- **Bundle issue lifecycle** (#1717): opened 2026-06-22 bundling #1715 + #1716. Both got superseded. All four bundle PRs merged by `an-lee`. Bundle now redundant.
- **`TokenAsset` (tested 2026-06-26)**: PORO at `app/models/token_asset.rb`. Balance = `token["balance"].to_r / 10**token["decimals"].to_i` (round 8 dp), or 0 if `token.blank?`. `balance_usd = (balance * currency.price_usd.to_f).round(4)`. Delegates 9 accessors to currency. `attr_reader :contract` is declared but `@contract` is never assigned — returns `nil`.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory (no callback fires). Do NOT call `Currency#update!` in tests.
- **`User#create_action` returns a boolean**. `Action#after_create :notify_target` is the production hook. `Action` extends `ActiveRecord::Base` directly (not `ApplicationRecord`), polymorphic `target`/`user` with `optional: true`.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. A non-empty array of blanks is NOT blank, but `compact_blank!` filters the blanks and the body continues.
- **`Tagging#articles_count` counter cache**: declared on `Tagging#belongs_to :tag, counter_cache: :articles_count`.
- **`HtmlPostProcessor#transform` signature**: `transform(css, &block)` — takes a CSS selector and a block; the block receives each matched Nokogiri element and mutates it in place. Returns `self` so the existing chain `.parse_paragraph.parse_link.parse_table...` works without changes.
- **`Payment#pre_order` lookup**: `PreOrder.find_by(follow_id: decoded_memo["f"])` — the memo's `f` field must match the pre-order's `follow_id`. In tests, build the pre-order first (to get its `follow_id`), then build the payment with `memo:` set explicitly to `build_payment_memo(type:, article:, follow_id: pre_order.follow_id)`.
- **`Transfer#snapshot_id` getter**: reads from the `snapshot` JSON column, not a top-level column. Tests that need a real `refund_transfer` must `Transfer.create!(..., snapshot: { "snapshot_id" => uuid })`.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`. The `recipient:` is positional, the rest are keyword args. The notifier `params[:...]` come from the keyword args.
