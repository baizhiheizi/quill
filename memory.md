---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-18 00:40 UTC (run 27737837357).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5).
- AGENTS.md exists; read before any code PR.

## Selected tasks for this run

- Task 5 (Coding Improvements) — removed `Order#broadcast_to_collection_views` (empty no-op) and the broken `data-controller='reload'` attribute from `app/views/shared/_reload.html.erb` (no `reload` controller exists in the Stimulus manifest, so every rescue → broadcast was logging a `Missing controller` warning). Branch `repo-assist/improve-remove-dead-order-broadcast-and-reload-controller` (commit c9469b9e) — 2 files changed, 2 insertions(+), 10 deletions(-). `bin/rails test test/models/` → 151 runs, 307 assertions, 0 failures, 0 errors. `bin/rails zeitwerk:check` → all is good. `bin/rubocop` → 481 files inspected, no offenses detected. Opened draft PR via `safeoutputs create_pull_request`. Patch: `/tmp/gh-aw/aw-repo-assist-improve-remove-dead-order-broadcast-and-reload-controller.patch` (1492 bytes, 50 lines). Bundle: `/tmp/gh-aw/aw-repo-assist-improve-remove-dead-order-broadcast-and-reload-controller.bundle` (935 bytes).
- Task 2 (Issue Investigation and Comment) — not applicable. All 7 open issues are auto-generated by github-actions[bot]. Fallback to Task 1 also not applicable (0 unlabelled issues at run start).
- Task 3 (Issue Investigation and Fix) — not applicable. No human-submitted open issues labelled `bug` / `help wanted` / `good first issue` to fix. The latent `Currency#store :raw` JSONB bug remains deferred — maintainer has not actioned prior attempts.
- Task 11 (Update Monthly Activity Summary) — added a run entry via `safeoutputs add_comment` on issue #1564 (temporary_id `aw_reload`). The `update_issue` body size and runner-queue issues from the prior run still apply; `add_comment` remains the reliable path.

## Completed this run

- Opened draft PR `repo-assist/improve-remove-dead-order-broadcast-and-reload-controller` (remove dead Order broadcast + broken reload Stimulus ref) via `safeoutputs create_pull_request`.
- Posted run entry on issue #1564 via `safeoutputs add_comment` (temporary_id `aw_reload`).

## Open issue landscape (7 open at run start, all auto-generated)

- #1667 (Authorization Boundary Hygiene quality report — 2026-06-15)
- #1636 ([aw] Detection Runs)
- #1567 ([aw] No-Op Runs)
- #1564 ([repo-assist] Monthly Activity 2026-06)
- #1561 ([efficiency-improver] Monthly Activity2026-06)
- #1517 ([test-improver] Monthly Activity 2026-06)
- #1513 ([perf-improver] Monthly Activity 2026-06)
- **No human-submitted issues to engage on.**

## Open PRs

- `repo-assist/improve-currency-test-coverage` (prior run) — Currency test coverage. Branch head 2e326078. 1 file, 53 insertions, 3 deletions. Test status: 10 runs, 15 assertions, 0 failures, 0 errors. Rubocop clean. Zeitwerk clean. PR number will be assigned by the workflow runner from the patch.
- `repo-assist/improve-remove-dead-order-broadcast-and-reload-controller` (this run) — remove dead Order broadcast + broken reload Stimulus ref. Branch head c9469b9e. 2 files changed, 2 insertions(+), 10 deletions(-). Test status: 151 model tests pass, 0 failures, 0 errors. Rubocop clean. Zeitwerk clean. PR number will be assigned by the workflow runner from the patch.

## Backlog / future work

- Re-engage when human issues appear.
- Watch for first-time contributors; welcome them and point to README/CONTRIBUTING.
- 5 notifiers in `app/notifiers/` still lack dedicated tests: `collection_listed`, `payment_refunded`, `subscribe_user_action_created`, `swap_order_finished`, `swap_order_swapping`. Test-improver is the natural owner; defer.
- **Latent `Currency#store :raw` JSONB bug, confirmed reproducible in prior runs**: calling `currency.name` or `currency.icon_url` on a freshly loaded record raises `TypeError: no implicit conversion of Hash into String`. The fix pattern (manual accessors) exists in memory but the maintainer has not actioned prior attempts; not re-attempted.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- The natural notifier-testing / efficiency / perf work is handled by other workflows (test-improver, efficiency-improver, perf-improver) at higher fidelity than repo-assist can match.
- #1667 (Authorization Boundary Hygiene) has a larger strategic set of auth gaps that the maintainer-led fix did not touch. Not appropriate for repo-assist to act unilaterally on auth/permission refactors — needs a maintainer-led design discussion before further PRs.
- The 858b5779 production Solid Cache/Cable/Queue SQLite migration opens up future work to verify there's no SQLITE_BUSY contention with multi-container Kamal deploys, but that's a maintainer-led production hardening question.
- The `_reload.html.erb` partial is still essentially a no-op placeholder; the rescue path it backs (`PG::InvalidParameterValue` in Order#broadcast_to_article_views and PreOrder#broadcast_to_views) just logs the error and renders an empty div. If a more user-visible recovery is wanted (e.g., a real toast), that's a follow-up feature, not a cleanup.

## Notes

- `gh aw compile` is available in this run container (`/usr/bin/gh` 2.94.0 + `gh aw` extension). Compile produces a `.patch` + `.bundle` artefact under `/tmp/gh-aw/`, and the PR is finalised by the workflow runner downstream via `safeoutputs create_pull_request`.
- `safeoutputs create_pull_request` returns the patch path and bundle size; the actual PR number is assigned by the workflow runner, so PR-intent items in Suggested Actions reference the branch + commit + patch path rather than a PR number.
- `update_issue` body size limit is 10 KB (10240 bytes). Prior run's body draft was ~11.5 KB on the first attempt — too large. Trimmed to ~9.6 KB and resubmitted; call returned success, but the subsequent `issue_read` showed the **old** body, suggesting the workflow runner queues body updates asynchronously (or `update_issue` from `schedule` / `workflow_dispatch` is silently no-op'ing). The Monthly Activity update is therefore being delivered via `add_comment` (temporary_id `aw_reload`) for reliability.
- `bin/rubocop` and `bin/rails zeitwerk:check` now run successfully locally — the prior `ruby-vips` environment limitation resolved.
- `bin/rubocop` does **not** lint ERB files (no `erb-lint` configured). When verifying a change that touches only ERB, skip the file from the rubocop invocation — the project's CI uses the same config.
- All 7 open issues remain auto-generated by github-actions[bot] (the 4 per-workflow Monthly Activity summaries, 2 [aw] detection/no-op trackers, and the 2026-06-15 Authorization Boundary Hygiene quality report).
