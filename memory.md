---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-14 04:50 UTC (run 27479845652).**
- Repo is baizhiheizi/quill (Rails 8.1, Ruby 4.0.5).
- AGENTS.md exists; read before any code PR.

## Selected tasks for this run

- Task 3 (Issue Investigation and Fix) — done: continued untested-notifier hygiene series with `TransferProcessedNotifier` (11 tests, 208 lines, single new file). Branch `repo-assist/test-transfer-processed-notifier-2026-06-14` committed (`716ed3dc`); draft PR opened via `safeoutputs create_pull_request`; patch at `/tmp/gh-aw/aw-repo-assist-test-transfer-processed-notifier-2026-06-14.patch`.
- Task 5 (Coding Improvements) — folded into Task 3; same PR (continued hygiene series).
- Task 2 (Issue Investigation and Comment) — not applicable: 9 open issues all auto-generated by `github-actions[bot]`; no human-submitted issues, no human activity to engage on. Fell back to Task 1, also a no-op (all issues labelled).
- Task 11 (Update Monthly Activity Summary) — done: rewrote issue #1564 with this run's entry prepended to Run History; Suggested Actions updated — added #1640 (PaymentCreatedNotifier) and the TransferProcessedNotifier intent; OrderCreatedNotifier intent removed (PR #1628 is now merged).

## Completed this run

- Verified PR #1628 `[repo-assist] test: add OrderCreatedNotifier tests` is now MERGED (merged 2026-06-14 02:58:51 UTC). Removed from Suggested Actions.
- Created branch `repo-assist/test-transfer-processed-notifier-2026-06-14` with 11 tests for `TransferProcessedNotifier` (208 lines, single new file).
- Tests pass: 11 runs, 32 assertions, 0 failures, 0 errors, 1 skip (data payload test blocked by `Currency#store :raw` + JSONB incompatibility; documented in the test with a clear follow-up note).
- Rubocop clean, Zeitwerk clean.
- Opened draft PR via `safeoutputs create_pull_request`; patch at `/tmp/gh-aw/aw-repo-assist-test-transfer-processed-notifier-2026-06-14.patch` (real PR number available after this workflow run exits).
- Updated issue #1564 `[repo-assist] Monthly Activity 2026-06` with this run's entry prepended.

## Open issue landscape (9 open at run start, all auto-generated)

- #1637, #1636, #1629, #1615, #1567, #1564, #1563, #1557, #1517, #1513 — see last-run memory; 9 of these were open at run start (the rest are closed/older)
- **No human-submitted issues to engage on.**

## Open PRs

- 1 Repo Assist PR: branch `repo-assist/test-transfer-processed-notifier-2026-06-14` (intent recorded via `safeoutputs create_pull_request`; real PR number available after this workflow run exits). Pending CI confirmation.
- Other open PRs observed: #1640 (PaymentCreatedNotifier from previous run, draft), #1639 (docs notifier coverage matrix), #1638 (docs surface count), #1635 (test-improver article distribution memo), #1634 (perf-improver Users::Scopable LEFT JOIN), #1633 (docs unbloat notifiers), #1632 (efficiency-improver infinite scroll).
- No stale non-Repo-Assist PRs.

## Backlog / future work

- Re-engage when human issues appear.
- Watch for first-time contributors; welcome them and point to README/CONTRIBUTING.
- 6 notifiers in `app/notifiers/` still lack dedicated tests after this run: `collection_bought`, `collection_listed`, `payment_refunded`, `subscribe_user_action_created`, `swap_order_finished`, `swap_order_swapping`. Each is a small (~30-80-line) follow-up if a test-improver run hasn't picked them up. Already-tested: `application`, `article_bought`, `article_published` (#1616), `article_rewarded`, `comment_created`, `comment_deleted`, `order_created` (#1628), `payment_created` (#1640), `tagging_created`, `transfer_processed` (this run), `user_connected`, `user_safe_registration`.
- Latent model bug surfaced: `app/models/currency.rb` `store :raw` on a JSONB column uses Rails' default YAML coder and raises `TypeError: no implicit conversion of Hash into String` when read in test. Fix: add `coder: JSON` to the `store :raw, accessors: %i[name icon_url change_usd]` declaration. This would unblock the `transfer_processed_notifier` `data` payload test and the `currency.icon_url` view paths in `app/views/transfers/`, `app/views/admin/transfers/`, etc.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work — do not touch AASM locks, encryption, Solid Queue topology, error reporting, or Stimulus error states until the quality-improver comes back to verify.
- Defer new engineering work until #1603 (API reference accuracy consolidation) lands.

## Notes

- All open issues carry the `agentic-workflows` label (or are per-workflow Monthly Activity summaries), so Task 1 (labelling) is not applicable.
- `safeoutputs` PR creation emits a `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR is finalised by the workflow runner downstream.
- The perf-improver and efficiency-improver workflows cover the perf/efficiency backlog at higher fidelity than repo-assist can match; defer to them.
- `update_issue` body size limit is 10 KB; trimmed historical run entries from full detail to one-line summaries to fit. The June issue body is now ~8.1 KB and has the current run plus 7 prior runs preserved.
- `update_issue` operation `replace` is the only fully-supported body operation that respects the size limit; `append` and `prepend` keep the existing body and so would not allow reformatting.
- TransferProcessedNotifier coverage added this run: 0 → 11 tests; first notifier test that uses a real `MixinNetworkUser` wallet (so `from_quill_bot?` returns false); first notifier test that exercises `may_notify_via_mixin_bot?` directly across all three branches (from_quill_bot, mixin_bot opt-out, default).
- Noticed 3 evaluates `config.if` at *delivery time*, not at *enqueue time* — this is why the `assert_no_enqueued_jobs only: DeliveryMethods::MixinBot` pattern from #1616/#1628 leaves 4 pre-existing failures. The same caveat applies to TransferProcessedNotifier; the from_quill_bot and mixin_bot opt-out cases are verified at the `may_notify_via_mixin_bot?` predicate level instead.
- The data payload test (`notification.data` exposing `APP_CARD` shape) is intentionally skipped with a clear comment pointing at the `Currency#store :raw` + JSONB incompatibility. Fixing this is a one-line model change (add `coder: JSON` to `store :raw`).
