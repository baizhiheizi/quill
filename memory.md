---
name: repo-assist-memory
description: Repo Assist run state — selected tasks, completed work, open backlog, monthly issue number, and notes
metadata:
  type: project
---

# Repo Assist Memory

## Current state

- **Run of repo-assist workflow on 2026-06-28 13:25 UTC (run 28321469137).**
- Repo: baizhiheizi/quill (Rails 8.1, Ruby 4.0.5). AGENTS.md exists; read before any code PR.
- **Open PRs from Repo Assist**: 1 (draft, this run). Branch `repo-assist/test-users-scopable-concern-2026-06-28` (commit `5aca265f`). Patch + bundle: `/tmp/gh-aw/aw-test-users-scopable-concern-2026-06-28.{patch,bundle}` (8228 / 2931 bytes). 1 other open PR: #1760 (perf-improver draft, Order#notify_subscribers SQL subquery).
- **Open issues**: 9 — all auto-generated by `github-actions[bot]`. 0 human-submitted open issues.
- **Dependabot queue**: clear. PR #1736 (annotaterb 4.22.0→4.23.0) merged 2026-06-26. Bundle #1717 REDUNDANT — all 4 PRs merged individually by `an-lee`. Recommend closing.
- **#1564 body** ~9.5 KB. Continued using `add_comment`. Comment `aw_yMSlG9tg` posted for this run.

## Selected tasks for run 28321469137 (2026-06-28 13:25 UTC)

- **Task 9 (Testing Improvements)** — DONE. Added `test/models/concerns/users/scopable_test.rb` (13 tests / 54 assertions). `Users::Scopable` is the 2nd-most-leveraged model concern per #1744 (after SoftDeletable). 9 of 13 scopes pinned: `with_authorization` (preload SELECT + N+1 guard), `only_blocked`/`without_blocked` (disjoint + union), `only_mixin_messenger`/`only_fennec`/`only_mvm` (provider filters via `joins(:authorization)`), `only_email_verified`/`only_validated` (independent predicates). The 4 `order_by_*` aggregate scopes are already pinned in `user_test.rb`. Branch `repo-assist/test-users-scopable-concern-2026-06-28`, commit `5aca265f`, +205/-0. `bundle exec rails test test/models/concerns/users/scopable_test.rb` → 13/54/0/0/0. Full `test/models/` → 277/668/0/0/1 (pre-existing skip). `bundle exec rubocop test/models/concerns/users/scopable_test.rb` → no offenses. `bundle exec rails zeitwerk:check` → all is good. Opened draft PR via `safeoutputs create_pull_request`.
- **Task 4 (Engineering Investments)** — DONE (no-op). Dependabot queue clear; no actionable gem updates or CI gaps identified. `bundle outdated` not runnable due to read-only gem cache.
- **Task 2 (Issue Investigation and Comment)** — DONE (no-op). All 9 open issues are auto-generated by `github-actions[bot]`. 0 human-submitted.
- **Task 11** — posted run entry on issue #1564 (comment `aw_yMSlG9tg`). Body at ~9.5 KB; continued using `add_comment` rather than rewriting.

## Completed this run (28321469137)

- Created branch `repo-assist/test-users-scopable-concern-2026-06-28` (commit `5aca265f`) with `test/models/concerns/users/scopable_test.rb`.
- Opened draft PR via `safeoutputs create_pull_request`.
- Posted run entry on issue #1564 (comment `aw_yMSlG9tg`).
- No human issues to fix or comment on. No Dependabot queue action needed.

## Prior runs (compressed, last 14 days)

- **28302089997** (2026-06-27 21:30 UTC): SoftDeletable concern tests → PR #1758.
- **28294779111** (2026-06-27 16:33 UTC): MixpayPreOrder tests → PR #1756.
- **28278300590** (2026-06-27 04:30 UTC): Action model tests → PR #1754/#1753.
- **28253287054** (2026-06-26 17:14 UTC): notifier tests → PR #1746.
- **28237903823** (2026-06-26 12:38 UTC): `HtmlPostProcessor#transform` → PR #1745.
- **28226940061** (2026-06-26 09:31 UTC): `CreateTagService` → PR #1743.
- **28217495965** (2026-06-26 04:53 UTC): `TokenAsset` → PR #1740.
- **27987716241** (2026-06-22 23:06 UTC): Dependabot bundle #1717 + `SubdomainConstraint` #1718.
- **27723165917** (2026-06-17 22:50 UTC): `Currency` test stub filled → PR #1682.

## Backlog / future work

- Re-engage when human issues appear. All 9 open issues are auto-generated.
- **Notifier test directory at 100% file coverage** (16 notifiers / 16 test files).
- **`MixpayPreOrder` (tested 2026-06-27)** — STI subclass of `PreOrder`. `:ensure_mixpay_supported` calls `item.mixpay_supported?` only. `pay_url` includes `payeeId`, `settlementAssetId`, `quoteAssetId`, `quoteAmount`, `traceId`, `settlementMemo`, `returnTo=pre_order_url(follow_id)`.
- **`SoftDeletable` (tested 2026-06-27)** — used by `Comment` and `AccessToken`. Three scopes, `deleted?` predicate, four actions. `AccessToken.kept` aliases `without_deleted`.
- **`Action` model coverage (PR #1754/#1753 merged 2026-06-27)** — polymorphic + optional `user`/`target`, `#notifications`, `destroy_notifications` cascade + `rescue StandardError`, `notify_target` dispatches `SubscribeUserActionCreatedNotifier` only for `action_type == :subscribe && target.is_a?(User)`.
- **`CollectionListedNotifier` (tested 2026-06-26)**: APP_CARD data exposes icon_url, title (36), description (72), action URL.
- **`PaymentRefundedNotifier` (tested 2026-06-26)**: `t(".refunded", item: ...)`. URL = mixin snapshot URL. PLAIN_TEXT. **`memo:` must be set explicitly on `Payment.new`**.
- **Remaining untested model concerns** (per #1744): `articles/content_preview`, `articles/poster_generator` (Grover), `pre_orders/swappable`, `users/mvm_queriable` (MVM cache), `users/email_verifiable`, `authenticatable`, `rich_text_content`, `orders/distributable`. **Best next candidate**: `articles/content_preview` (scope helper) or `rich_text_content`.
- **Latent `Currency#store :raw` JSONB bug** — partially addressed by maintainer; defer further action.
- Respect issue #1571's 5-cycle cooldown on payment/Web3 resilience work.
- #1667 + #1686 + #1694 are maintainer-led design discussions; out of scope.
- 858b5779 production Solid Cache/Cable/Queue SQLite migration → future SQLITE_BUSY contention check (maintainer-led).
- `Admin::CommentsController#show` removed in PR #1721. Editor layout Twitter handle fixed in PR #1724.
- `modal-component`, `load-more`, `session` Stimulus controllers are used.
- **Bundle #1717 is REDUNDANT** — all four PRs merged individually. Recommend closing.
- **#1564 body** at ~9.5 KB. `update_issue` limit is 10 KB. Continue using `add_comment` until body exceeds 10 KB or maintainer requests a rewrite.

## Notes

- `gh aw compile` produces `.patch` + `.bundle` artefact under `/tmp/gh-aw/`; PR finalised via `safeoutputs create_pull_request`.
- `bin/rubocop` does NOT lint ERB or YAML. `bin/rails zeitwerk:check` is the local equivalent of the CI load-path check.
- **Stimulus dead-code detection**: scan `data-controller="xxx"`, `data-controller='xxx'`, `controller: 'xxx'`. "Dead" only if (a) no view references it AND (b) the controller file does not exist.
- **Empty controller stub detection**: a Stimulus controller with only a `connect()` that does `console.log(...)` is a stub left from a migration.
- **Dead Rails action detection**: for any `def <verb>` on a controller, confirm the verb is exposed by the matching route AND a view file exists.
- **Admin filter typo pattern**: `where(col_a: params[:col_b], col_b: params[:col_b])` — copy-paste where the second binding uses the wrong param key.
- **Pre-existing test errors in CI-clean code**: `test/controllers/articles_controller_test.rb` (3 errors, asset pipeline) and `test/controllers/pre_orders_controller_test.rb` (2+ errors, SSL to Mixpay). CI stubs/supplies them.
- **`remote_image_tag` helper** (added in `ef58c599`): returns `nil` for nil/blank or non-absolute-URL source, otherwise calls `image_tag`. Translates `lazy: true` to `loading: "lazy"`.
- **`User#available_articles` perf pattern**: push `.uniq` to SQL via `.or.or.distinct`. Verified set-equivalent on `Reader Two` (id 45371440).
- **`includes(:currency).sum(...)` antipattern**: Rails silently falls back to a JOIN when `.sum` references another table's column. Use `joins(:currency)`.
- **`HomeController#active_authors` perf pattern**: `where.not(id: current_user.block_user_ids)` materializes the full block list. perf-improver PR #1735 uses `NOT IN (SELECT ...)` SQL subquery.
- **`setTimeout(fn(), ms)` antipattern**: invoking `fn()` synchronously then scheduling `undefined`. Fixed in `cbd10abd`.
- **Bundle issue lifecycle** (#1717): all four bundle PRs merged by `an-lee`. Bundle now redundant.
- **`TokenAsset`** (PORO at `app/models/token_asset.rb`): Balance = `token["balance"].to_r / 10**token["decimals"].to_i` (round 8 dp), or 0 if `token.blank?`. Delegates 9 accessors to currency. `attr_reader :contract` declared but `@contract` never assigned — returns `nil`.
- **`Currency#save` raises in test env** because `before_validation :set_defaults` calls `QuillBot.api.asset(asset_id)`. Tests use `Currency.new(price_usd: ...)` in memory. Do NOT call `Currency#update!` in tests.
- **`User#create_action` returns a boolean** (action_store gem). `Action#after_create :notify_target` is the production hook.
- `Settings.twitter_account` is `prsdigg` in `config/settings/test.yml`. `QuillBotStub#with_quill_bot_stub` provides a `FakeApi` with `FAKE_CLIENT_ID`.
- `config/routes.rb` includes a domain redirect for `prsdigg.com|bunshow.jp` → `https://quill.im/$path` at the top; preserve when refactoring.
- `ERB::Util.url_decode` does not exist in Rails 8.1; use `CGI.unescape`.
- **`CreateTagService` subtle behavior**: `tag_names.blank?` short-circuits only for `nil` and `[]`. A non-empty array of blanks is NOT blank, but `compact_blank!` filters the blanks and the body continues.
- **`Tagging#articles_count` counter cache**: declared on `Tagging#belongs_to :tag, counter_cache: :articles_count`.
- **`HtmlPostProcessor#transform` signature**: `transform(css, &block)` — takes a CSS selector and a block; the block receives each matched Nokogiri element and mutates it in place. Returns `self`.
- **`Payment#pre_order` lookup**: `PreOrder.find_by(follow_id: decoded_memo["f"])`.
- **`Transfer#snapshot_id` getter**: reads from the `snapshot` JSON column, not a top-level column. Tests need `Transfer.create!(..., snapshot: { "snapshot_id" => uuid })`.
- **`NotifierHelpers#deliver_notifier!`** signature: `deliver_notifier!(notifier_class, record:, recipient:, **params)`. The `recipient:` is positional, the rest are keyword args.
- **`safeoutputs create_pull_request` returns success but the branch may not reach `origin` immediately**: in 28302089997, 28294779111 (MixpayPreOrder), 28278300590 (action-model), 28270555934 (efficiency-improver). The push appears to happen out-of-band. The maintainer should pull the patch/bundle from `/tmp/gh-aw/aw-<branch-slug>.{patch,bundle}` only if no PR appears within a day.
- **Concern testing pattern**: when a concern is included by ≥2 models, write the test through one canonical instance and one aliased-scopes instance (e.g. `Comment` + `AccessToken` where `kept` is an alias for `without_deleted`). Avoid mocking the callback chain — exercise real callbacks to keep tests honest.
- **`test/models/concerns/` directory** now has 3 test files (soft_deletable, users/statable, users/scopable). Per #1744, 8 of 12 model concerns remain untested. Best next candidate: `articles/content_preview` (simple scope helper) or `rich_text_content`.
- **`Users::Scopable` (tested 2026-06-28)** — used by `User`. 13 scopes including `with_authorization` (preload), `only_blocked`/`without_blocked`, `only_mixin_messenger`/`only_fennec`/`only_mvm` (provider filters via `joins(:authorization)`), `active`, `only_email_verified`/`only_validated`, and 4 aggregate `order_by_*`. Provider-filter scopes write `where(authorization: { provider: ... })` which only resolves once a JOIN brings in `user_authorizations` aliased as `authorization`. In production, `Admin::UsersController` chains `with_authorization` (`includes(:authorization)`), and Rails switches `includes` to eager-load `LEFT OUTER JOIN` once the WHERE references the included table's columns.