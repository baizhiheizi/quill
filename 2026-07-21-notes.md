# Test Improver Run — 2026-07-21

- Workflow run: https://github.com/baizhiheizi/quill/actions/runs/29864269229
- Tasks: Task 6 (test infrastructure — testing guide), Task 2 (identify new opportunities), Task 7 (Monthly Activity update)
- Branch: `test-assist/testing-guide`, commit `602c8a9`

## Task 6: Testing Infrastructure — Centralized Testing Guide

**Created `test/TESTING_GUIDE.md`** consolidating ~40 testing gotchas from 23+ runs into a single reference document. This was proposed in issue #1934 (infrastructure improvements).

Organized by category:
1. **Commands** — validated build/test/lint commands
2. **Test Support** — which utilities exist and where (`with_quill_bot_stub`, `stub_class_method`, etc.)
3. **Fixtures & Test Data** — Order baseline, User uid, Article creation patterns
4. **Database Constraints** — idx_orders unique constraint, Bonus table-name bug, encrypted pin
5. **Callback Behavior** — before_validation firing (on every valid?), save(validate: false), after_initialize set_defaults, after_commit enqueue, AASM quirks
6. **Stubbing & Mocking** — Mocha unavailable, define_singleton_method safety, QuillBot stubs, MixinBot stubs, belongs_to freshness
7. **Model-Specific Notes** — all non-trivial models with gotchas
8. **Noticed/Notifications** — STI column, record denormalization
9. **Controller/Integration Tests** — IntegrationTestCase, API pattern
10. **Untested Areas** — AdvisoryLockable, RichTextContent, Localizable concerns; 17 Dashboard controllers

Draft PR created via safeoutputs (branch `test-assist/testing-guide`).

## Task 2: New Testing Opportunities Identified

With model-level coverage substantially complete, new opportunities include:

### Controllers (High Value)
- **17 untested Dashboard controllers** — 413 LOC total. Most substantial: collections (64 LOC), profile_settings (41 LOC), transfers (40 LOC), articles (33 LOC)

### Model Concerns
- **AdvisoryLockable** — PostgreSQL advisory lock pattern (27 LOC, non-trivial lock logic)
- **RichTextContent** — content rendering branching logic (100 LOC, used by Article)

### Controller Concerns
- **Localizable** — Accept-Language header parsing (41 LOC, quality-value sorting)
- **UserFieldPreloads**, **RenderingHelper**, **API::RenderingHelper** (smaller)

## Memory update
- Added new gotcha to MEMORY.md: `test/TESTING_GUIDE.md` created
- Updated backlog with new areas found during this run's scan
